# searchX — 项目约定

通用深度调研引擎，并自动发布为公开信息流站。三个能力：`/research`（通用调研，`.claude/skills/research/SKILL.md`）、`/stock`（单只股票深度投研，`.claude/skills/stock/SKILL.md`；research 判定为股票类时自动转交给它）——这两者发布到公开站；以及 `/factcheck`（事实核查，`.claude/skills/factcheck/SKILL.md`——核实真假 + 讲清原委 + 判断可信度，支持文本 / 图片 / 链接），**产出仅存本机 Obsidian、不上线**。围绕前两者有一条半自动流水线（站内提交 → 审核 → runner 自动跑 → 发布上线 → 邮件），代码在 `services/` 与 `web/`。完整地图见 `README.md`。

## 路径变量（SKILL 引用）

> 两个变量的**本机绝对路径**定义在未入库的 `CLAUDE.local.md`（Claude Code 自动加载、不进公开仓库）。下面只说明用途。

- `ARCHIVE_ROOT` — 调研资产根（仓库内 `research/`）。每次调研在此下建独立主题文件夹（`<YYYY-MM-DD>_<topic-slug>/`），存全部资产。
- `OBSIDIAN_VAULT` — 本机 Obsidian 库根。调研**全文笔记（中文文件名）**存 `Research/` 子目录（由 `scripts/report-to-obsidian.js` 从 `report.html` 转出），事实核查笔记存 `Factcheck/` 子目录，均带 frontmatter 与 `[[]]` 双链。

## 仓库结构速览

- `.claude/skills/{research,stock,factcheck}/` — 三个 skill（能力本体 + `research/templates/report.html` 报告模板，stock 复用同模板；factcheck 产出 Obsidian 笔记、无模板、不上线）。
- `research/` — 调研资产库（= `ARCHIVE_ROOT`），每主题一文件夹（三件套）+ `INDEX.md` 总索引；也是站点数据源。
- `web/` — 信息流站（`src` 源 / `build` 构建逻辑 / `dist` 产物，CI 自动部署）。
- `services/intake-worker/` · `services/runner/` · `services/check-runner/` · `services/stocks-import/` — 半自动流水线（提交 worker / 自动跑研究 + 发信 / 自动跑核查落 Obsidian / 每日搬运 Stocks 的个股深度调研），各有独立 README。
- `docs/` — 开发文档（设计稿 / 计划 / 进度，见 `docs/README.md`）。

## 全局约定（所有任务适用）

- **输出语言**：中文。科技/学术对象优先检索英文一手来源，再用中文消化。
- **信息源优先级**：监管/交易所 ＞ 公司披露/官方 filing ＞ 权威媒体 ＞ 机构研究 ＞ 社区。
- **时间**：所有时点用北京时间。
- **隐私红线（绝对）**：任何可导出文档（HTML / md / 来源清单）中，永远不写入用户私人信息（持仓规模、负债与还款、财务状况、健康、家庭）。涉及买卖/持有用条件化表述。
- **数据完整性**：让数据直接说话；数据与既有结论冲突时立即修正解读，不为维护结论而扭曲。
- **不预设深层意图**：只处理用户明确指定的对象，不擅自外延。
- **X/Twitter 局限**：无法稳定拉取账号完整时间线（免费 API 已关闭 + 反爬严格）。人物类调研重点研究被引用/转载/整理的代表性观点与方法论，并在产出中明确声明这一局限。
- **状态量必须核到当日**（2026-08-10 教训）。把事实分两类：**历史事实**（某年营收、某日发生了什么）核到当初那条来源就够，来源多久以前都不影响；**状态量**（是否上市 / 在申请 / 已获批 / 仍在任 / 目前第一）则**以来源日期为有效期上限**——引一条 N 个月前的报道描述"现在如何"，等于顺带断言这 N 个月没变过，这个断言得自己去证实。凡写现状表述，必须补一次带当前年月的检索并写明「截至 YYYY-MM-DD」；补不到就退成「截至〈来源日期〉时」，不许当现状陈述。**这类错躲得过一切"回到所标来源核对"式的检查**（来源当初确实那么写），所以要单独过一遍。
- **一次查空不等于不存在**。查「长鑫存储」在行情库里是空的，注册名「长鑫科技」才查得到，于是报告写成了"未上市"——实际它 14 天前就已挂牌。查不到时先换名称、换代码、换模糊匹配、换库/换源，都试过再下结论；尤其**别把"我没查到"写成"不存在 / 没有 / 未发生"**。

## 改代码时的约定

> 来自 2026-07-31 那两轮全项目审查的实际教训。细节见 `docs/ARCHITECTURE.md` 第 8 节（改动前必读）。

- **改完要再审一遍自己的改动**。那次修完 68 个已核验的缺陷、测试全绿、也上线了；第二轮专门复审同一批改动，其中 **15 个是修复本身引入的新问题**。改动量大、或动的是锁 / 状态落盘 / 校验这类"出错就静默"的代码时，收尾必须再过一轮，立场是推翻"已经修好了"，而不是接着找新问题。
- **加一层保护之前，先问是不是拆掉了另一层**。给锁加"运行期间定期更新时间戳"解决了"长批次被误判超时"，同时也让"进程活着但卡死"永远不再被判超时——用一层保护换掉另一层，是最难发现的一类回归。重试、降级、兜底这三类改动尤其要问这句。
- **测试夹具绿不等于真实数据对**。这是本项目最主要的缺陷形态：改 `web/build/`、`scripts/report-to-obsidian.js`、查重逻辑之后，必须写临时脚本对 `research/` 下**全部存量**真跑一遍再下结论。临时脚本一律写进系统临时目录，且输出路径显式传绝对路径（有代理把未定义变量拼进路径，在仓库根跑出过残留目录）。
- **界面改动必须在真浏览器里走一遍，异常路径也走**。纯函数测试测不到接线：有一次降级提示被写进了随表单一起隐藏的元素，函数测试全绿、页面上完全看不见，等于没改。
- **"已修复 / 已验证"都是待核实的说法**，无论写在文档、注释、提交信息还是记忆里。本项目有过多次被实测推翻的先例（架构文档断言"现有笔记没问题"，实测 35 篇里 7 篇线上早就在显示错误内容）。结论只认代码与实测；审查代理给的具体数字和版本断言同样要自己复现一遍再采信。
- **shell 脚本要同时考虑 macOS 与 Linux**：本机是 macOS、CI 是 Linux，`stat` 取修改时间的参数完全不同（`-f %m` / `-c %Y`），而 GNU 的 `-f` 不会报错、会打印挂载点，`|| 兜底` 拦不住。解析 git 输出里的文件名一律加 `-c core.quotePath=false`，否则中文路径会变成八进制转义串，所有按文件名做的判断全部失效（隐私红线上的机密文件名匹配就这样失效过）。
- **锁文件、临时目录这类路径用固定值 + 环境变量可覆盖**，别用 `$TMPDIR`：不同触发方式（ssh、launchd）拿到的值不一样，互斥会形同虚设，而且测试也没法隔离。

> 下面几条来自 2026-08-10 的修复。

- **试连要查真表，别用"看着能通"的命令**。行情库文档里的 `?mode=ro` 在这个 WAL 库上常态打不开（真实查询报错 14），可 `SELECT 1` 不碰数据页、照样返回成功——用它试连会得到"库能用"的假象，于是这条取数通道断了很久没人发现。**探活语句必须走到真实数据**（查一张真表、拿一个真值），凡是"轻量探测"都要先问它会不会在坏掉时也成功。
- **换掉一个坏掉的机制时，说清楚新的弱在哪**。`mode=ro` 是打开级只读、`query_only` 是连接级只读，后者弱一层。换是对的（前者根本查不动、等于把通道整条堵死），但**代价要写进文档**，否则下一个人会以为保证没变。
- **一个检索引擎给出的"0 条"不等于"没有"**。Pagefind 的中文分词把「江丰电子」切成「江丰」+「电子」再按 AND 匹配，「江丰」不在索引里就整条落空，站上明明有那篇报告。**面向用户的检索至少要有两条独立通路**（现在是 Pagefind 全文 + `reports.json` 清单直配），任何一条返回空都不该表现成"查无此项"。
- **改共享默认值，先 grep 有没有别处写死了这个字面量**。查重窗口 30→20 时，权威常量只有一处、改起来一行，但 `config.test.js` 里把 30 写成了字面量，直接挂两条测试；文档里另有七八处文字要跟着同步。改完 grep 一遍旧值，测试里一律引用常量而不是抄数字。

## 五大常关注板块（双链与关联判断用）

光模块 · 机器人 · 算力 · AI应用 · 航天。仅在确有关联时挂 `related` / 双链，不硬凑。
