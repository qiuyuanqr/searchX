# M1 信息流站 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

> **⚠️ 执行前结构更新（2026-06-03 敲定，覆盖下文旧路径）**
>
> 为便于 M2/M3 长大后管理，所有"做网站"的东西归入 `web/`，并预留 `services/` 给 M2/M3。**下文各 Task 里出现的旧路径按下表替换**（脚本内的相对 import 不变，因为整目录平移）：
>
> | 旧路径（下文） | 新路径（实际建） |
> |---|---|
> | `build/`（构建脚本/测试/夹具） | `web/build/` |
> | `site-src/`（模板 + assets） | `web/src/` |
> | `site/`（构建产物，git 忽略） | `web/dist/` |
> | `research/`、`docs/`、`.claude/`、`.github/`、`package.json` | 不变（仍在根目录） |
> | M2/M3 的 worker/runner/emailer | 届时建在 `services/` 下 |
>
> 受影响处：`package.json` 的 `build`/`serve` 脚本（`web/build/cli.js`、`--site web/dist`、`cd web/dist`）；各 `bun test web/build/*.test.js`；测试与 `build()` 默认值里的 `root/out/assets/template`；`deploy.yml` 的 `path: web/dist`。
>
> **与现状对齐（下文 Task 1/9 部分步骤已过时）**：项目**已是 git 仓库**（Task 1 Step 1 的 `git init` 跳过）；`.gitignore` **已存在**（Task 1 Step 3 改为"更新"它：把 `site/` 改成 `web/dist/`）；**远程已建** `git@github.com:qiuyuanqr/searchX.git`（Task 9 Step 2 的 `gh repo create` 跳过，直接 push origin）；本机**未装 `gh`**（Task 9 开 Pages 用 GitHub 网页设置）。执行在功能分支 `m1-feed-site` 上进行，全部跑通后合回 `main` 触发部署。

**Goal:** 把 `research/` 下已生成的报告自动聚成一个仿纸张质感、可搜索、可上线 GitHub Pages 的静态信息流站。

**Architecture:** 一个 bun 构建脚本扫描 `research/<日期>_<slug>/`，依次完成：从 `notes.md` 解析元数据、把每份 `report.html` 复制进 `site/`、用模板渲染信息流首页、再用 Pagefind 建全文索引；GitHub Action 在 push 时构建并部署 Pages。前端为零框架的仿纸张质感单页（暖色纸底、衬线字体、大量留白；视觉已在 brainstorm 阶段定稿）。

**Tech Stack:** bun（运行 + 测试 `bun:test`）、gray-matter（解析 frontmatter）、Pagefind（静态全文检索）、GitHub Actions + Pages。

**实体形状（贯穿全程，属性名固定）：**
```js
// 一个调研条目
{
  dir: "2026-06-03_cpo-silicon-photonics-supply-chain", // research 下的文件夹名
  date: "2026-06-03",            // 取自 dir 前 10 字符
  slug: "cpo-silicon-photonics-supply-chain", // dir 第 12 字符起
  type: "板块",                   // frontmatter.type
  title: "CPO / 硅光产业链",       // notes.md 第一个 `# ` 标题
  tldr: "AI 算力撞上…",            // notes.md 第一个 `> ` 引用块
  tags: ["CPO","硅光"],           // frontmatter.tags
  boards: ["光模块","算力"],       // frontmatter.related 去掉 [[ ]]
  sourceCount: 14,               // frontmatter.source_count
  href: "r/2026-06-03_cpo-silicon-photonics-supply-chain/" // 站内链接
}
```

**视觉来源：** brainstorm 定稿原型 `.superpowers/brainstorm/sess/content/feed-paper-v4.html`（暖纸+衬线、卡片 3D 倾斜/悬浮/标题上移/按下时缩一下松开弹回、极细的渐变分隔线、整页滚动 + 回到顶部、深色模式、reduced-motion）。本计划 Task 7/8 已把它的 CSS/JS 完整内联，无需依赖该临时文件。

---

## Task 1: 项目脚手架

**Files:**
- Create: `package.json`
- Create: `.gitignore`
- Create: `build/sanity.test.js`

- [ ] **Step 1: 初始化 git 与 package.json**

Run:
```bash
cd /Users/yangqiuyuan/Coding/searchX
git init
```

Create `package.json`:
```json
{
  "name": "searchx-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build/cli.js && bunx pagefind --site site",
    "test": "bun test",
    "serve": "bunx pagefind --site site && python3 -m http.server 8080 --directory site"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "pagefind": "^1.1.0"
  }
}
```

- [ ] **Step 2: 安装依赖**

Run: `bun install`
Expected: 生成 `bun.lockb` 与 `node_modules/`，无报错。

- [ ] **Step 3: 写 .gitignore**

Create `.gitignore`:
```
node_modules/
site/
.superpowers/
.DS_Store
*.log
```

- [ ] **Step 4: 冒烟测试，确认 bun test 可用**

Create `build/sanity.test.js`:
```js
import { test, expect } from "bun:test";

test("bun test runs", () => {
  expect(1 + 1).toBe(2);
});
```

- [ ] **Step 5: 运行测试**

Run: `bun test build/sanity.test.js`
Expected: PASS（1 test）。

- [ ] **Step 6: 提交**

```bash
git add package.json .gitignore build/sanity.test.js bun.lockb
git commit -m "chore: scaffold searchx static site build (bun + gray-matter + pagefind)"
```

---

## Task 2: notes.md 解析器 `parseNote`

**Files:**
- Create: `build/parse-note.js`
- Test: `build/parse-note.test.js`

- [ ] **Step 1: 写失败测试**

Create `build/parse-note.test.js`:
```js
import { test, expect } from "bun:test";
import { parseNote } from "./parse-note.js";

const SAMPLE = `---
date: 2026-06-03
type: 板块
tags: [research, CPO, 硅光]
related: ["[[光模块]]", "[[算力]]"]
source_count: 14
archive: "research/2026-06-03_cpo-silicon-photonics-supply-chain/"
---

# CPO / 硅光产业链

> AI 算力撞上"功耗墙+带宽墙"后，光通信被迫往芯片身边搬家。

## 正文小节
内容……`;

test("parseNote 提取全部字段", () => {
  const e = parseNote(SAMPLE, "2026-06-03_cpo-silicon-photonics-supply-chain");
  expect(e.dir).toBe("2026-06-03_cpo-silicon-photonics-supply-chain");
  expect(e.date).toBe("2026-06-03");
  expect(e.slug).toBe("cpo-silicon-photonics-supply-chain");
  expect(e.type).toBe("板块");
  expect(e.title).toBe("CPO / 硅光产业链");
  expect(e.tldr).toContain("光通信被迫往芯片身边搬家");
  expect(e.tags).toEqual(["research", "CPO", "硅光"]);
  expect(e.boards).toEqual(["光模块", "算力"]);
  expect(e.sourceCount).toBe(14);
  expect(e.href).toBe("r/2026-06-03_cpo-silicon-photonics-supply-chain/");
});

test("parseNote 容错：缺字段不崩", () => {
  const e = parseNote("---\ntype: 概念\n---\n\n正文无标题", "2026-06-02_x");
  expect(e.title).toBe("x");        // 退化为 slug
  expect(e.tldr).toBe("");
  expect(e.boards).toEqual([]);
  expect(e.sourceCount).toBe(0);
});
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `bun test build/parse-note.test.js`
Expected: FAIL（`parse-note.js` 不存在 / 函数未定义）。

- [ ] **Step 3: 实现 `parseNote`**

Create `build/parse-note.js`:
```js
import matter from "gray-matter";

export function parseNote(raw, dir) {
  const { data, content } = matter(raw);
  const titleMatch = content.match(/^#\s+(.+)$/m);
  const tldrMatch = content.match(/^>\s?(.+(?:\n>.*)*)/m);

  const title = titleMatch ? titleMatch[1].trim() : dir.slice(11);
  const tldr = tldrMatch ? tldrMatch[1].replace(/\n>\s?/g, " ").trim() : "";
  const boards = (data.related || []).map(
    (s) => String(s).replace(/\[\[|\]\]/g, "").trim()
  );

  return {
    dir,
    date: dir.slice(0, 10),
    slug: dir.slice(11),
    type: data.type || "",
    title,
    tldr,
    tags: data.tags || [],
    boards,
    sourceCount: data.source_count || 0,
    href: `r/${dir}/`,
  };
}
```

- [ ] **Step 4: 运行测试，确认通过**

Run: `bun test build/parse-note.test.js`
Expected: PASS（2 tests）。

- [ ] **Step 5: 提交**

```bash
git add build/parse-note.js build/parse-note.test.js
git commit -m "feat: parseNote — extract feed metadata from notes.md"
```

---

## Task 3: 扫描器 `scanResearch`

**Files:**
- Create: `build/scan.js`
- Test: `build/scan.test.js`
- Create (fixtures): `build/fixtures/research/2026-06-01_alpha/notes.md`, `build/fixtures/research/2026-06-02_beta/notes.md`, `build/fixtures/research/_not-a-topic/notes.md`

- [ ] **Step 1: 建测试夹具**

Create `build/fixtures/research/2026-06-01_alpha/notes.md`:
```
---
date: 2026-06-01
type: 概念
tags: [a]
related: ["[[算力]]"]
source_count: 5
---

# Alpha 概念

> Alpha 的一句话结论。
```

Create `build/fixtures/research/2026-06-02_beta/notes.md`:
```
---
date: 2026-06-02
type: 板块
tags: [b]
related: []
source_count: 9
---

# Beta 板块

> Beta 的一句话结论。
```

Create `build/fixtures/research/_not-a-topic/notes.md`:
```
不符合 日期_slug 命名，应被忽略。
```

- [ ] **Step 2: 写失败测试**

Create `build/scan.test.js`:
```js
import { test, expect } from "bun:test";
import { scanResearch } from "./scan.js";

const ROOT = "build/fixtures/research";

test("只收 日期_slug 文件夹，按日期倒序", () => {
  const entries = scanResearch(ROOT);
  expect(entries.length).toBe(2);
  expect(entries.map((e) => e.dir)).toEqual([
    "2026-06-02_beta",
    "2026-06-01_alpha",
  ]);
});

test("条目带解析后的字段", () => {
  const beta = scanResearch(ROOT)[0];
  expect(beta.title).toBe("Beta 板块");
  expect(beta.sourceCount).toBe(9);
  expect(beta.href).toBe("r/2026-06-02_beta/");
});
```

- [ ] **Step 3: 运行测试，确认失败**

Run: `bun test build/scan.test.js`
Expected: FAIL（`scan.js` 不存在）。

- [ ] **Step 4: 实现 `scanResearch`**

Create `build/scan.js`:
```js
import { readdirSync, statSync, readFileSync, existsSync } from "fs";
import { join } from "path";
import { parseNote } from "./parse-note.js";

const DIR_RE = /^\d{4}-\d{2}-\d{2}_/;

export function scanResearch(root) {
  return readdirSync(root)
    .filter(
      (name) =>
        DIR_RE.test(name) &&
        statSync(join(root, name)).isDirectory() &&
        existsSync(join(root, name, "notes.md"))
    )
    .map((name) =>
      parseNote(readFileSync(join(root, name, "notes.md"), "utf8"), name)
    )
    .sort((a, b) =>
      a.date < b.date ? 1 : a.date > b.date ? -1 : a.dir < b.dir ? 1 : -1
    );
}
```

- [ ] **Step 5: 运行测试，确认通过**

Run: `bun test build/scan.test.js`
Expected: PASS（2 tests）。

- [ ] **Step 6: 提交**

```bash
git add build/scan.js build/scan.test.js build/fixtures
git commit -m "feat: scanResearch — collect+sort entries from research dir"
```

---

## Task 4: 卡片渲染 `renderCard`

**Files:**
- Create: `build/render-card.js`
- Test: `build/render-card.test.js`

- [ ] **Step 1: 写失败测试**

Create `build/render-card.test.js`:
```js
import { test, expect } from "bun:test";
import { renderCard, escapeHtml } from "./render-card.js";

const ENTRY = {
  dir: "2026-06-03_cpo",
  date: "2026-06-03",
  slug: "cpo",
  type: "板块",
  title: "CPO / 硅光产业链",
  tldr: "一句话结论 <带尖括号>",
  tags: ["CPO"],
  boards: ["光模块", "算力"],
  sourceCount: 14,
  href: "r/2026-06-03_cpo/",
};

test("卡片含标题、类型、链接、筛选用 data 属性", () => {
  const html = renderCard(ENTRY);
  expect(html).toContain('href="r/2026-06-03_cpo/"');
  expect(html).toContain("CPO / 硅光产业链");
  expect(html).toContain('data-type="板块"');
  expect(html).toContain('data-boards="光模块,算力"');
  expect(html).toContain("14 来源");
  expect(html).toContain("2026 · 06 · 03");
});

test("转义用户文本里的尖括号", () => {
  expect(escapeHtml("<a>")).toBe("&lt;a&gt;");
  expect(renderCard(ENTRY)).toContain("&lt;带尖括号&gt;");
});
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `bun test build/render-card.test.js`
Expected: FAIL（`render-card.js` 不存在）。

- [ ] **Step 3: 实现 `renderCard` + `escapeHtml`**

Create `build/render-card.js`:
```js
export function escapeHtml(s) {
  return String(s)
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;");
}

export function renderCard(e) {
  const dateDisp = e.date.replace(/-/g, " · ");
  const boards = e.boards.join(" · ");
  const srcs = e.sourceCount ? `${e.sourceCount} 来源` : "";
  return `<li class="article-card" data-type="${escapeHtml(e.type)}" data-boards="${escapeHtml(e.boards.join(","))}">
  <a class="card-link" href="${escapeHtml(e.href)}">
    <div class="card-top"><span class="ctype">${escapeHtml(e.type)}</span><span class="cdate">${escapeHtml(dateDisp)}</span></div>
    <h2 class="card-title">${escapeHtml(e.title)}</h2>
    <p class="lead">${escapeHtml(e.tldr)}</p>
    <div class="card-foot"><span class="boards">${escapeHtml(boards)}</span><span class="srcs">${escapeHtml(srcs)}</span></div>
  </a>
</li>`;
}
```

- [ ] **Step 4: 运行测试，确认通过**

Run: `bun test build/render-card.test.js`
Expected: PASS（2 tests）。

- [ ] **Step 5: 提交**

```bash
git add build/render-card.js build/render-card.test.js
git commit -m "feat: renderCard — entry to paper feed card HTML (escaped)"
```

---

## Task 5: 首页渲染 `renderIndex`

**Files:**
- Create: `build/render-index.js`
- Test: `build/render-index.test.js`

- [ ] **Step 1: 写失败测试**

Create `build/render-index.test.js`:
```js
import { test, expect } from "bun:test";
import { renderIndex } from "./render-index.js";

const TPL = `<ul class="article-list"><!-- CARDS --></ul>`;
const ENTRIES = [
  { dir: "2026-06-02_b", date: "2026-06-02", slug: "b", type: "板块", title: "B 标题", tldr: "b", tags: [], boards: [], sourceCount: 1, href: "r/2026-06-02_b/" },
  { dir: "2026-06-01_a", date: "2026-06-01", slug: "a", type: "概念", title: "A 标题", tldr: "a", tags: [], boards: [], sourceCount: 2, href: "r/2026-06-01_a/" },
];

test("把卡片注入占位符，保留顺序", () => {
  const html = renderIndex(ENTRIES, TPL);
  expect(html).not.toContain("<!-- CARDS -->");
  expect(html).toContain("B 标题");
  expect(html).toContain("A 标题");
  expect(html.indexOf("B 标题")).toBeLessThan(html.indexOf("A 标题"));
});
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `bun test build/render-index.test.js`
Expected: FAIL（`render-index.js` 不存在）。

- [ ] **Step 3: 实现 `renderIndex`**

Create `build/render-index.js`:
```js
import { renderCard } from "./render-card.js";

export function renderIndex(entries, template) {
  const cards = entries.map(renderCard).join("\n");
  return template.replace("<!-- CARDS -->", cards);
}
```

- [ ] **Step 4: 运行测试，确认通过**

Run: `bun test build/render-index.test.js`
Expected: PASS（1 test）。

- [ ] **Step 5: 提交**

```bash
git add build/render-index.js build/render-index.test.js
git commit -m "feat: renderIndex — inject cards into page template"
```

---

## Task 6: 构建编排 `build()` + CLI

**Files:**
- Create: `build/build.js`
- Create: `build/cli.js`
- Test: `build/build.test.js`
- 依赖前置文件（Task 7 才写正式版，这里先放最小模板/资产占位以便测试）：
  - Create: `site-src/index.template.html`（最小版，Task 7 替换为定稿）
  - Create: `site-src/assets/feed.css`（空占位，Task 7 填）
  - Create: `site-src/assets/feed.js`（空占位，Task 8 填）
- 测试夹具报告：`build/fixtures/research/2026-06-01_alpha/report.html`, `build/fixtures/research/2026-06-02_beta/report.html`

- [ ] **Step 1: 放最小模板与空资产（Task 7/8 会替换内容）**

Create `site-src/index.template.html`:
```html
<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><title>searchX · 调研档案</title>
<link rel="stylesheet" href="assets/feed.css"></head>
<body><ul class="article-list"><!-- CARDS --></ul>
<script src="assets/feed.js"></script></body></html>
```

Create empty `site-src/assets/feed.css`:
```css
/* 占位，Task 7 填入定稿样式 */
```

Create empty `site-src/assets/feed.js`:
```js
/* 占位，Task 8 填入交互+搜索 */
```

- [ ] **Step 2: 给夹具加 report.html**

Create `build/fixtures/research/2026-06-01_alpha/report.html`:
```html
<!doctype html><html><head><meta charset="utf-8"><title>Alpha 概念</title></head><body><h1>Alpha 概念</h1><p>alpha 正文。</p></body></html>
```

Create `build/fixtures/research/2026-06-02_beta/report.html`:
```html
<!doctype html><html><head><meta charset="utf-8"><title>Beta 板块</title></head><body><h1>Beta 板块</h1><p>beta 正文。</p></body></html>
```

- [ ] **Step 3: 写失败测试**

Create `build/build.test.js`:
```js
import { test, expect, afterAll } from "bun:test";
import { build } from "./build.js";
import { existsSync, readFileSync, rmSync } from "fs";

const OUT = "build/fixtures/out";
afterAll(() => rmSync(OUT, { recursive: true, force: true }));

test("build 产出首页、报告副本、资产", () => {
  const entries = build({
    root: "build/fixtures/research",
    out: OUT,
    assets: "site-src/assets",
    template: "site-src/index.template.html",
  });
  expect(entries.length).toBe(2);
  expect(existsSync(`${OUT}/index.html`)).toBe(true);
  expect(existsSync(`${OUT}/r/2026-06-02_beta/index.html`)).toBe(true);
  expect(existsSync(`${OUT}/assets/feed.css`)).toBe(true);

  const home = readFileSync(`${OUT}/index.html`, "utf8");
  expect(home).toContain("Beta 板块");
  expect(home).not.toContain("<!-- CARDS -->");

  const report = readFileSync(`${OUT}/r/2026-06-02_beta/index.html`, "utf8");
  expect(report).toContain("beta 正文");
});
```

- [ ] **Step 4: 运行测试，确认失败**

Run: `bun test build/build.test.js`
Expected: FAIL（`build.js` 不存在）。

- [ ] **Step 5: 实现 `build()`**

Create `build/build.js`:
```js
import {
  rmSync, mkdirSync, cpSync, readFileSync, writeFileSync, existsSync,
} from "fs";
import { join } from "path";
import { scanResearch } from "./scan.js";
import { renderIndex } from "./render-index.js";

export function build({
  root = "research",
  out = "site",
  assets = "site-src/assets",
  template = "site-src/index.template.html",
} = {}) {
  rmSync(out, { recursive: true, force: true });
  mkdirSync(out, { recursive: true });

  const entries = scanResearch(root);

  for (const e of entries) {
    const destDir = join(out, "r", e.dir);
    mkdirSync(destDir, { recursive: true });
    cpSync(join(root, e.dir, "report.html"), join(destDir, "index.html"));
    const dataDir = join(root, e.dir, "data");
    if (existsSync(dataDir)) cpSync(dataDir, join(destDir, "data"), { recursive: true });
  }

  const tpl = readFileSync(template, "utf8");
  writeFileSync(join(out, "index.html"), renderIndex(entries, tpl));
  cpSync(assets, join(out, "assets"), { recursive: true });

  return entries;
}
```

- [ ] **Step 6: 运行测试，确认通过**

Run: `bun test build/build.test.js`
Expected: PASS（1 test）。

- [ ] **Step 7: 写 CLI 入口**

Create `build/cli.js`:
```js
import { build } from "./build.js";

const entries = build();
console.log(`Built ${entries.length} entries → site/`);
```

- [ ] **Step 8: 跑一次真实构建（暂不含 Pagefind）**

Run: `bun run build/cli.js`
Expected: 打印 `Built 4 entries → site/`（当前 4 篇真实报告），生成 `site/index.html` 与 `site/r/*/index.html`。

- [ ] **Step 9: 全量测试 + 提交**

Run: `bun test`
Expected: 全部 PASS。
```bash
git add build/build.js build/cli.js build/build.test.js site-src build/fixtures
git commit -m "feat: build orchestration — scan, render index, copy reports+assets"
```

---

## Task 7: 前端定稿（模板 + feed.css + 筛选/交互 JS）

**Files:**
- Modify: `site-src/index.template.html`（替换为定稿）
- Modify: `site-src/assets/feed.css`（填入定稿样式）
- Modify: `site-src/assets/feed.js`（填入交互；搜索在 Task 8）

> 验证方式：前端为表现层，用"构建 + 浏览器目视 + 控制台无错"验收，不做单元测试。

- [ ] **Step 1: 写定稿首页模板**

Replace `site-src/index.template.html` with:
```html
<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>searchX · 调研档案</title>
<link rel="stylesheet" href="assets/feed.css">
</head>
<body data-pagefind-ignore>
  <div class="wrap">
    <header>
      <div class="kicker"><span class="seal">研</span> 深度调研引擎 · DEEP RESEARCH</div>
      <h1 class="brand">searchX · 调研档案</h1>
      <div class="search-wrap"><input id="q" class="search" placeholder="搜索题目、结论、标签…" autocomplete="off"></div>
      <div class="chips" id="chips">
        <span class="chip on" data-filter="all">全部</span>
        <span class="chip" data-filter="type:概念">概念</span>
        <span class="chip" data-filter="type:人物">人物</span>
        <span class="chip" data-filter="type:方法论">方法论</span>
        <span class="chip" data-filter="type:事件">事件</span>
        <span class="chip" data-filter="type:板块">板块</span>
        <span class="chip" data-filter="board:光模块">· 光模块</span>
        <span class="chip" data-filter="board:算力">· 算力</span>
      </div>
      <div class="rule-grad"></div>
    </header>

    <ul class="article-list" id="feed"><!-- CARDS --></ul>
    <div id="results" class="results" hidden></div>
    <div id="empty" class="empty" hidden>没有匹配的条目。</div>
  </div>

  <button class="to-top" aria-label="回到顶部" title="回到顶部">↑</button>
  <script type="module" src="assets/feed.js"></script>
</body>
</html>
```

- [ ] **Step 2: 填入定稿样式（来自 v4 原型，含 results/empty 新样式）**

Replace `site-src/assets/feed.css` with:
```css
:root{
  --paper:#f7f4ee; --paper-2:#efe9df; --ink:#2c2823; --ink-soft:#5f594f;
  --muted:#9c9486; --rule:#e7e1d5; --hair:rgba(70,60,48,.08); --seal:#b14a31; --seal-soft:#c97a62; --card:#fcfaf5;
  --sans:ui-sans-serif,-apple-system,"PingFang SC","Hiragino Sans GB",sans-serif;
  --serif:"Iowan Old Style","New York",Palatino,"Palatino Linotype",Georgia,"Songti SC","Noto Serif CJK SC",serif;
}
@media (prefers-color-scheme: dark){
  :root{
    --paper:#17150f; --paper-2:#1e1b14; --ink:#efe9dc; --ink-soft:#c0b9a8;
    --muted:#857d6f; --rule:#332f25; --hair:rgba(240,233,220,.07); --seal:#e57c61; --seal-soft:#c97a62; --card:#1d1a13;
  }
}
*{box-sizing:border-box; margin:0; padding:0}
html{scroll-behavior:smooth}
body{background:var(--paper); color:var(--ink); font-family:var(--serif);
  font-size:18px; line-height:1.7; -webkit-font-smoothing:antialiased; text-rendering:optimizeLegibility}
.wrap{max-width:46rem; margin:0 auto; padding:1.9rem 1.5rem 5.5rem}
.kicker{font-family:var(--sans); font-size:.68rem; letter-spacing:.22em; text-transform:uppercase; color:var(--muted); display:flex; align-items:center; gap:.55rem}
.seal{display:inline-flex; align-items:center; justify-content:center; width:1.35rem; height:1.35rem; border:1.4px solid var(--seal); color:var(--seal); border-radius:3px; font-size:.6rem; font-weight:700; transform:rotate(-4deg)}
h1.brand{font-family:var(--serif); font-size:1.95rem; font-weight:600; letter-spacing:-.015em; margin:.45rem 0 0}
.search-wrap{position:relative; margin:1.2rem 0 .95rem}
.search-wrap::before{content:"🔍"; position:absolute; left:13px; top:50%; transform:translateY(-50%); font-size:.82rem; opacity:.45}
.search{width:100%; font-family:var(--sans); font-size:.92rem; color:var(--ink); background:var(--card);
  border:1px solid var(--rule); border-radius:10px; padding:.65rem .9rem .65rem 2.3rem; outline:none; transition:border-color .2s, box-shadow .2s}
.search::placeholder{color:var(--muted)}
.search:focus{border-color:var(--seal-soft); box-shadow:0 0 0 3px rgba(177,74,49,.09)}
.chips{display:flex; flex-wrap:wrap; gap:.42rem; font-family:var(--sans); font-size:.76rem; margin-bottom:1rem}
.chip{padding:.26rem .78rem; border:1px solid var(--rule); border-radius:20px; color:var(--ink-soft); cursor:pointer; transition:all .2s; background:transparent; user-select:none}
.chip:hover{border-color:var(--seal-soft); color:var(--seal); transform:translateY(-1px)}
.chip.on{background:var(--seal); border-color:var(--seal); color:#fff}
.rule-grad{height:1px; background:linear-gradient(90deg, transparent 0%, var(--hair) 16%, var(--hair) 84%, transparent 100%)}
.article-list{list-style:none; margin-top:1.5rem}
@keyframes riseIn{from{opacity:0; transform:translateY(16px)} to{opacity:1; transform:none}}
.article-card{position:relative; animation:riseIn .58s cubic-bezier(.2,.7,.2,1) backwards}
.article-card:nth-child(1){animation-delay:.04s}
.article-card:nth-child(2){animation-delay:.11s}
.article-card:nth-child(3){animation-delay:.18s}
.article-card:nth-child(4){animation-delay:.25s}
.article-card.hide{display:none}
.article-card:not(:last-child)::after{content:""; display:block; height:1px; margin:1.5rem 1.4rem;
  background:linear-gradient(90deg, transparent 0%, var(--hair) 30%, var(--hair) 70%, transparent 100%)}
.card-link{position:relative; display:block; text-decoration:none; color:inherit;
  background:var(--card); border-radius:13px; padding:1.35rem 1.5rem 1.25rem; box-shadow:0 1px 2px rgba(0,0,0,.03);
  transform:perspective(900px) rotateX(var(--rx,0deg)) rotateY(var(--ry,0deg));
  transition:transform .18s ease, box-shadow .32s ease; will-change:transform}
.card-link::before{content:""; position:absolute; left:0; top:16px; bottom:16px; width:3px; border-radius:3px;
  background:var(--seal); transform:scaleY(0); transform-origin:center; opacity:0; transition:transform .3s ease, opacity .3s ease}
.card-link:hover{box-shadow:0 14px 30px rgba(0,0,0,.11);
  transform:perspective(900px) rotateX(var(--rx,0deg)) rotateY(var(--ry,0deg)) translateY(-4px) scale(1.012)}
.card-link:hover::before{transform:scaleY(1); opacity:.9}
.card-link:active{transition:transform .08s ease; transform:perspective(900px) translateY(-1px) scale(.993)}
.card-top{display:flex; justify-content:space-between; align-items:center; font-family:var(--sans);
  font-size:.67rem; letter-spacing:.14em; text-transform:uppercase}
.ctype{color:var(--seal); font-weight:700}
.cdate{color:var(--muted); letter-spacing:.1em}
.card-title{font-family:var(--serif); font-size:1.42rem; line-height:1.22; font-weight:600; letter-spacing:-.01em;
  margin:.5rem 0 .38rem; display:inline-block; transition:color .22s ease, transform .22s ease}
.card-link:hover .card-title{color:var(--seal); transform:translateY(-2px)}
.lead{color:var(--ink-soft); font-size:.98rem; line-height:1.6; margin:0}
.card-foot{margin-top:.95rem; display:flex; justify-content:space-between; align-items:center;
  font-family:var(--sans); font-size:.75rem; color:var(--muted)}
.srcs{font-variant-numeric:tabular-nums}
.results{margin-top:1.5rem; display:flex; flex-direction:column; gap:1.1rem}
.result a{display:block; text-decoration:none; color:inherit; background:var(--card); border-radius:13px;
  padding:1.1rem 1.4rem; box-shadow:0 1px 2px rgba(0,0,0,.03); transition:box-shadow .25s ease, transform .18s ease}
.result a:hover{box-shadow:0 12px 26px rgba(0,0,0,.1); transform:translateY(-3px)}
.result h3{font-family:var(--serif); font-size:1.2rem; font-weight:600; margin-bottom:.3rem}
.result .ex{color:var(--ink-soft); font-size:.92rem; line-height:1.55}
.result .ex mark{background:rgba(177,74,49,.18); color:inherit; padding:0 .1em; border-radius:2px}
.empty{margin-top:2rem; text-align:center; color:var(--muted); font-family:var(--sans); font-size:.9rem}
.to-top{position:fixed; right:20px; bottom:20px; width:44px; height:44px; border-radius:50%;
  background:var(--card); border:1px solid var(--rule); color:var(--seal); font-size:1.15rem;
  display:flex; align-items:center; justify-content:center; cursor:pointer; z-index:50;
  box-shadow:0 4px 14px rgba(0,0,0,.1); opacity:0; transform:translateY(10px); pointer-events:none;
  transition:opacity .3s ease, transform .3s ease, box-shadow .2s ease, border-color .2s ease}
.to-top.show{opacity:1; transform:none; pointer-events:auto}
.to-top:hover{transform:translateY(-3px); box-shadow:0 9px 22px rgba(0,0,0,.15); border-color:var(--seal-soft)}
.to-top:active{transform:translateY(-1px) scale(.95)}
@media (prefers-reduced-motion: reduce){
  html{scroll-behavior:auto}
  .article-card{animation:none}
  .card-link, .card-title{transition:none !important; transform:none !important}
}
```

- [ ] **Step 3: 写交互 JS（倾斜 + 回到顶部 + 标签筛选；搜索在 Task 8 追加）**

Replace `site-src/assets/feed.js` with:
```js
const reduce = window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches;

// 卡片 3D 倾斜
function bindTilt(){
  if (reduce) return;
  const MAX = 4;
  document.querySelectorAll(".card-link").forEach((card) => {
    card.addEventListener("pointermove", (e) => {
      const r = card.getBoundingClientRect();
      const px = (e.clientX - r.left) / r.width;
      const py = (e.clientY - r.top) / r.height;
      card.style.setProperty("--ry", ((px - 0.5) * 2 * MAX).toFixed(2) + "deg");
      card.style.setProperty("--rx", (-(py - 0.5) * 2 * MAX).toFixed(2) + "deg");
    });
    card.addEventListener("pointerleave", () => {
      card.style.setProperty("--rx", "0deg");
      card.style.setProperty("--ry", "0deg");
    });
  });
}

// 回到顶部
function bindToTop(){
  const toTop = document.querySelector(".to-top");
  const firstCard = document.querySelector(".article-card");
  function onScroll(){
    const t = firstCard ? (firstCard.offsetTop + firstCard.offsetHeight / 3) : 140;
    (window.scrollY > t) ? toTop.classList.add("show") : toTop.classList.remove("show");
  }
  window.addEventListener("scroll", onScroll, { passive: true });
  onScroll();
  toTop.addEventListener("click", () => window.scrollTo({ top: 0, behavior: reduce ? "auto" : "smooth" }));
}

// 标签筛选（type: / board:）
function bindChips(){
  const chips = document.getElementById("chips");
  const cards = [...document.querySelectorAll("#feed .article-card")];
  const empty = document.getElementById("empty");
  chips.addEventListener("click", (e) => {
    const chip = e.target.closest(".chip");
    if (!chip) return;
    chips.querySelectorAll(".chip").forEach((c) => c.classList.remove("on"));
    chip.classList.add("on");
    const f = chip.dataset.filter;
    let shown = 0;
    cards.forEach((card) => {
      let ok = f === "all";
      if (f.startsWith("type:")) ok = card.dataset.type === f.slice(5);
      if (f.startsWith("board:")) ok = (card.dataset.boards || "").split(",").includes(f.slice(6));
      card.classList.toggle("hide", !ok);
      if (ok) shown++;
    });
    empty.hidden = shown > 0;
  });
}

bindTilt();
bindToTop();
bindChips();
```

- [ ] **Step 4: 构建并目视验收**

Run:
```bash
bun run build/cli.js
cd site && python3 -m http.server 8080
```
打开 `http://localhost:8080`，确认：仿纸张质感外观、4 张真实卡片、悬浮倾斜/标题变红、滚动出现回到顶部按钮、点筛选条按类型/板块过滤、深色模式（系统切换）正常、浏览器控制台无报错。看完 `Ctrl-C` 停掉。

- [ ] **Step 5: 提交**

```bash
git add site-src
git commit -m "feat: finalized paper feed UI (template + css + tilt/scroll/filter js)"
```

---

## Task 8: Pagefind 全文检索

**Files:**
- Modify: `site-src/assets/feed.js`（追加搜索逻辑）
- 说明：`package.json` 的 `build` 脚本已含 `bunx pagefind --site site`（Task 1）。

- [ ] **Step 1: 先生成一次 Pagefind 索引**

Run: `bun run build`（= 构建 + `bunx pagefind --site site`）
Expected: 生成 `site/pagefind/`（含 `pagefind.js`）。首次会下载 pagefind 二进制。

- [ ] **Step 2: 在 feed.js 追加搜索逻辑**

Append to `site-src/assets/feed.js`:
```js
// Pagefind 全文检索：输入即查，清空回到信息流
function debounce(fn, ms){ let t; return (...a) => { clearTimeout(t); t = setTimeout(() => fn(...a), ms); }; }

function bindSearch(){
  const input = document.getElementById("q");
  const feed = document.getElementById("feed");
  const results = document.getElementById("results");
  const chips = document.getElementById("chips");
  const empty = document.getElementById("empty");
  let pf;

  async function ensure(){
    if (!pf) { pf = await import("/pagefind/pagefind.js"); await pf.init(); }
    return pf;
  }
  function showFeed(){ results.hidden = true; results.innerHTML = ""; feed.hidden = false; chips.hidden = false; }
  function showResults(){ feed.hidden = true; chips.hidden = true; results.hidden = false; }

  const run = debounce(async (q) => {
    if (!q) { showFeed(); empty.hidden = true; return; }
    const engine = await ensure();
    const search = await engine.search(q);
    const items = await Promise.all(search.results.slice(0, 20).map((r) => r.data()));
    if (!items.length) { showResults(); results.innerHTML = ""; empty.hidden = false; return; }
    empty.hidden = true;
    results.innerHTML = items.map((d) =>
      `<div class="result"><a href="${d.url}"><h3>${d.meta.title || "(无标题)"}</h3><p class="ex">${d.excerpt}</p></a></div>`
    ).join("");
    showResults();
  }, 180);

  input.addEventListener("input", (e) => run(e.target.value.trim()));
}

bindSearch();
```

- [ ] **Step 3: 重新构建并验收搜索**

Run:
```bash
bun run build
cd site && python3 -m http.server 8080
```
打开 `http://localhost:8080`：输入一个正文里的词（如「激光器」），确认出现结果卡片、点进是对应报告；清空输入框回到信息流；控制台无错。`Ctrl-C` 停。

- [ ] **Step 4: 提交**

```bash
git add site-src/assets/feed.js
git commit -m "feat: Pagefind full-text search wired into paper feed"
```

---

## Task 9: GitHub Pages 自动部署

**Files:**
- Create: `.github/workflows/deploy.yml`

- [ ] **Step 1: 写部署工作流**

Create `.github/workflows/deploy.yml`:
```yaml
name: Deploy site
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - run: bun run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 2: 提交并创建公开仓库、推送**

Run:
```bash
git add .github/workflows/deploy.yml
git commit -m "ci: build + deploy site to GitHub Pages on push"
git branch -M main
gh repo create searchX --public --source=. --remote=origin --push
```
Expected: 仓库创建并推送成功（`gh` 已登录；未登录先 `gh auth login`）。

- [ ] **Step 3: 开启 Pages = GitHub Actions 源**

在 GitHub 仓库 Settings → Pages → Build and deployment → Source 选 **GitHub Actions**（或 `gh api -X POST repos/:owner/searchX/pages -f build_type=workflow`）。

- [ ] **Step 4: 验证线上**

Run: `gh run watch`（等部署完成）
Expected: workflow 绿；访问 `https://<用户名>.github.io/searchX/` 看到信息流、能点进报告、能搜索。

> 注：仓库名作为子路径会影响绝对路径 `/pagefind/...`。若站点部署在 `/searchX/` 子路径下，Pagefind 的 `import("/pagefind/pagefind.js")` 需改为相对路径 `import("./pagefind/pagefind.js")`（因首页在站点根）。**验收时若搜索 404，把 feed.js 里这一处与 result 链接改成相对路径并重建。** 用自定义域名或用户主页仓库（`<用户名>.github.io`）则无此问题。

- [ ] **Step 5（按需修正子路径）**

若上一步搜索 404：把 `site-src/assets/feed.js` 中 `import("/pagefind/pagefind.js")` 改为 `import("./pagefind/pagefind.js")`，提交推送，`gh run watch` 重验。
```bash
git add site-src/assets/feed.js && git commit -m "fix: relative pagefind path for project-pages subpath" && git push
```

---

## Task 10: 真实内容验收（M1 完成定义）

- [ ] **Step 1: 本地全流程跑通**

Run: `bun test && bun run build`
Expected: 测试全绿；`site/` 含 4 篇报告 + `pagefind/`。

- [ ] **Step 2: 对照 spec §13 M1 验收清单逐条确认**
- [ ] 信息流呈现全部条目（4 篇），按日期倒序
- [ ] 卡片点进是对应 `report.html`
- [ ] 搜索能命中正文（如「激光器」「卡脖子」）
- [ ] 类型/板块筛选可用
- [ ] 深色模式 / reduced-motion 正常
- [ ] Pages 线上可访问，朋友凭链接可看

- [ ] **Step 3: 最终提交（如有微调）**

```bash
git add -A && git commit -m "chore: M1 feed site acceptance pass" && git push
```

---

## 自查（写完即查）

- **Spec 覆盖**：§5.1 内容模型→T2/T3；§5.2 构建→T2–T6；§5.3 UI→T7；§5.4 搜索（Pagefind 打底）→T8（自然语言检索属未来，spec §5.4/§11 已标）；§5.5 部署→T9；§13 M1 验收→T10。✅ 无遗漏（NL 语义检索为 spec 明确推迟项，不在 M1）。
- **占位扫描**：T6 的"空占位资产"是有意的、随后在 T7/T8 被完整内容替换，非计划占位；其余步骤均含可运行代码/命令。✅
- **类型一致**：实体属性名（dir/date/slug/type/title/tldr/tags/boards/sourceCount/href）在 T2–T6 一致；`renderCard`/`renderIndex`/`build`/`scanResearch` 命名前后一致；feed.js 的 `#feed/#results/#chips/#empty/#q/.to-top` 与模板一致。✅
