# 首页与详情页重设计 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 体验导向地重做信息流站首页与报告详情页，解决手机端、找内容、长报告导航、信息密度四个痛点，且不破坏现有暖色衬线阅读身份。

**Architecture:** 首页改在 `web/build`（卡片/索引渲染）+ `web/src`（模板/样式/前端逻辑）；详情页改在 `web/build/inject-report-nav.js`（构建时注入，存量报告即生效）。板块沿用 `related`→`boards`，显示/筛选都取与 5 大板块的交集，不新增 frontmatter 字段。筛选逻辑抽成纯函数单测。

**Tech Stack:** Bun（`bun test` / 构建脚本）、gray-matter、Pagefind、原生 ES module 前端、纯 HTML/CSS。

**对齐设计稿：** `docs/superpowers/specs/2026-06-27-首页与详情页重设计-设计.md`

**前置：** 当前已在分支 `redesign-feed-and-report`。所有提交落此分支，全绿后再合 main（合 main = 触发 CI 部署上线）。

---

## File Structure

新建：
- `web/build/boards.js` — 5 大板块单一常量 + 取交集工具。被 `render-card.js` 复用。
- `web/src/assets/feed-filter.js` — 首页筛选/计数/月分隔可见性的纯函数（浏览器 + 测试共用）。
- `web/src/assets/feed-filter.test.js` — 上者的单测。

修改：
- `web/build/render-card.js` / `render-card.test.js` — 紧凑卡片结构 + 全类型显示板块（交集）。
- `web/build/render-index.js` / `render-index.test.js` — 按月插入分隔行。
- `web/src/index.template.html` — 板块 chips、计数、吸顶筛选区。
- `web/src/assets/feed.css` — 紧凑卡片、吸顶、月分隔、计数、浮动按钮贴列、`--measure`、去 3D 倾斜。
- `web/src/assets/feed.js` — 两组筛选 AND + 计数 + 月分隔联动 + 搜索态切换；移除 `bindTilt`。
- `web/build/inject-report-nav.js` / `inject-report-nav.test.js` — 阅读进度条 + 自动目录（电脑侧栏 + 手机浮层 + 目录按钮）+ 按钮贴列。
- `.claude/skills/research/SKILL.md`、`.claude/skills/stock/SKILL.md` — 写明板块标签约定。

---

## Task 1: 板块常量 + 紧凑卡片 + 全类型显示板块

**Files:**
- Create: `web/build/boards.js`
- Modify: `web/build/render-card.js`
- Test: `web/build/render-card.test.js`

- [ ] **Step 1: 建板块常量模块**

Create `web/build/boards.js`:

```js
// 五大常关注板块（双链/筛选的唯一权威清单）。related 里可能混入非板块双链，
// 显示与筛选都只取与这 5 个的交集，避免脏标签外露。
export const BOARDS = ["光模块", "机器人", "算力", "AI应用", "航天"];
const SET = new Set(BOARDS);

// 从一条 entry 的 boards（= related 解析值）里筛出真正属于 5 大板块的项，保持原顺序。
export function boardsOf(boards) {
  return (boards || []).filter((b) => SET.has(b));
}
```

- [ ] **Step 2: 改 render-card 测试为「紧凑结构 + 全类型显示交集板块」**

Replace the whole body of `web/build/render-card.test.js` with:

```js
import { test, expect } from "bun:test";
import { renderCard, escapeHtml } from "./render-card.js";

const ENTRY = {
  dir: "2026-06-03_cpo",
  date: "2026-06-03",
  slug: "cpo",
  type: "板块",
  title: "CPO / 硅光产业链",
  tldr: "一句话结论 <带尖括号>",
  tags: ["CPO"],
  boards: ["光模块", "算力"],
  sourceCount: 14,
  href: "r/2026-06-03_cpo/",
};

test("卡片含标题、类型、链接、筛选用 data 属性", () => {
  const html = renderCard(ENTRY);
  expect(html).toContain('href="r/2026-06-03_cpo/"');
  expect(html).toContain("CPO / 硅光产业链");
  expect(html).toContain('data-type="板块"');
  expect(html).toContain('data-boards="光模块,算力"');
  expect(html).toContain("14 来源");
  expect(html).toContain("2026·06·03");
});

test("紧凑结构：类型徽标 + 标题 + 一句话 + 微元信息一行", () => {
  const html = renderCard(ENTRY);
  expect(html).toContain('class="ctype"');
  expect(html).toContain('class="card-title"');
  expect(html).toContain('class="lead"');
  expect(html).toContain('class="card-meta"');
});

test("转义用户文本里的尖括号", () => {
  expect(escapeHtml("<a>")).toBe("&lt;a&gt;");
  expect(renderCard(ENTRY)).toContain("&lt;带尖括号&gt;");
});

test("所有类型都在微元信息里显示板块（交集），不再仅限股票", () => {
  const html = renderCard(ENTRY); // type=板块
  expect(html).toContain("光模块 · 算力");
  const stock = renderCard({ ...ENTRY, type: "股票" });
  expect(stock).toContain("光模块 · 算力");
});

test("只显示属于 5 大板块的项，过滤掉 related 里的非板块双链", () => {
  const html = renderCard({ ...ENTRY, boards: ["光模块", "CPO 共封装光学", "概率"] });
  expect(html).toContain("光模块");
  expect(html).not.toContain("CPO 共封装光学");
  expect(html).not.toContain("概率");
  // data-boards 仍保留全部原始值（筛选只匹配 5 大板块，多余值无害）
  expect(html).toContain('data-boards="光模块,CPO 共封装光学,概率"');
});

test("无板块时微元信息不带板块段", () => {
  const html = renderCard({ ...ENTRY, boards: [] });
  expect(html).not.toContain(" · 光模块");
  expect(html).toContain("14 来源");
});

test("无一句话结论时不渲染 lead", () => {
  const html = renderCard({ ...ENTRY, tldr: "" });
  expect(html).not.toContain('class="lead"');
});
```

- [ ] **Step 3: 跑测试，确认失败**

Run: `bun test web/build/render-card.test.js`
Expected: FAIL（旧 renderCard 仍输出 `.card-top`/`.boards`，新断言不命中）

- [ ] **Step 4: 重写 render-card.js 为紧凑结构**

Replace the whole `web/build/render-card.js` with:

```js
import { boardsOf } from "./boards.js";

export function escapeHtml(s) {
  return String(s)
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;");
}

export function renderCard(e) {
  const dateDisp = e.date.replace(/-/g, "·"); // 2026-06-03 → 2026·06·03
  const boards = boardsOf(e.boards);
  const srcMeta = e.sourceCount ? ` · ${e.sourceCount} 来源` : "";
  const boardMeta = boards.length ? ` · ${escapeHtml(boards.join(" · "))}` : "";
  const lead = e.tldr ? `<p class="lead">${escapeHtml(e.tldr)}</p>` : "";
  // data-boards 仍写入全部原始 boards：筛选 chip 只匹配 5 大板块名，多余值无害且保留向后兼容。
  return `<li class="article-card" data-type="${escapeHtml(e.type)}" data-boards="${escapeHtml((e.boards || []).join(","))}">
  <a class="card-link" href="${escapeHtml(e.href)}">
    <span class="ctype">${escapeHtml(e.type)}</span>
    <div class="card-body">
      <h2 class="card-title">${escapeHtml(e.title)}</h2>
      ${lead}
      <div class="card-meta">${escapeHtml(dateDisp)}${srcMeta}${boardMeta}</div>
    </div>
  </a>
</li>`;
}
```

- [ ] **Step 5: 跑测试，确认通过**

Run: `bun test web/build/render-card.test.js`
Expected: PASS（全部用例绿）

- [ ] **Step 6: 提交**

```bash
git add web/build/boards.js web/build/render-card.js web/build/render-card.test.js
git commit -m "feat(web): 紧凑卡片 + 全类型显示板块（取 5 大板块交集）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: 信息流按月插入分隔行

**Files:**
- Modify: `web/build/render-index.js`
- Test: `web/build/render-index.test.js`

- [ ] **Step 1: 加月分隔测试**

Replace the whole `web/build/render-index.test.js` with:

```js
import { test, expect } from "bun:test";
import { renderIndex } from "./render-index.js";

const TPL = `<ul class="article-list"><!-- CARDS --></ul>`;
const mk = (date, title) => ({
  dir: `${date}_x`, date, slug: "x", type: "概念", title,
  tldr: "t", tags: [], boards: [], sourceCount: 1, href: `r/${date}_x/`,
});

test("把卡片注入占位符，保留顺序", () => {
  const html = renderIndex([mk("2026-06-02", "B 标题"), mk("2026-06-01", "A 标题")], TPL);
  expect(html).not.toContain("<!-- CARDS -->");
  expect(html.indexOf("B 标题")).toBeLessThan(html.indexOf("A 标题"));
});

test("跨月边界插入月分隔行，标签为「年 · 中文月」", () => {
  const html = renderIndex([mk("2026-06-24", "六月条目"), mk("2026-05-30", "五月条目")], TPL);
  expect(html).toContain('<li class="month-sep" data-month="2026-06">2026 · 六月</li>');
  expect(html).toContain('<li class="month-sep" data-month="2026-05">2026 · 五月</li>');
  // 分隔在对应卡片之前
  expect(html.indexOf("2026 · 六月")).toBeLessThan(html.indexOf("六月条目"));
  expect(html.indexOf("六月条目")).toBeLessThan(html.indexOf("2026 · 五月"));
});

test("同月多条只插一个分隔行", () => {
  const html = renderIndex([mk("2026-06-24", "甲"), mk("2026-06-20", "乙")], TPL);
  expect(html.match(/data-month="2026-06"/g).length).toBe(1);
});

test("空列表不产分隔行", () => {
  const html = renderIndex([], TPL);
  expect(html).not.toContain("month-sep");
});
```

- [ ] **Step 2: 跑测试，确认失败**

Run: `bun test web/build/render-index.test.js`
Expected: FAIL（当前 renderIndex 不产 `month-sep`）

- [ ] **Step 3: 实现按月分组**

Replace the whole `web/build/render-index.js` with:

```js
import { renderCard } from "./render-card.js";

const CN_MONTH = ["一", "二", "三", "四", "五", "六", "七", "八", "九", "十", "十一", "十二"];

function monthLabel(date) {
  const [y, m] = date.split("-");
  return `${y} · ${CN_MONTH[parseInt(m, 10) - 1]}月`;
}

// entries 已按新→旧排序（见 scan.compareByNewest）。跨月边界插一行月分隔。
export function renderIndex(entries, template) {
  let lastMonth = "";
  const parts = [];
  for (const e of entries) {
    const ym = e.date.slice(0, 7); // YYYY-MM
    if (ym !== lastMonth) {
      lastMonth = ym;
      parts.push(`<li class="month-sep" data-month="${ym}">${monthLabel(e.date)}</li>`);
    }
    parts.push(renderCard(e));
  }
  return template.replace("<!-- CARDS -->", parts.join("\n"));
}
```

- [ ] **Step 4: 跑测试，确认通过**

Run: `bun test web/build/render-index.test.js`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add web/build/render-index.js web/build/render-index.test.js
git commit -m "feat(web): 信息流按月插入分隔行

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: 首页筛选/计数/月分隔可见性纯函数

**Files:**
- Create: `web/src/assets/feed-filter.js`
- Test: `web/src/assets/feed-filter.test.js`

- [ ] **Step 1: 写纯函数测试**

Create `web/src/assets/feed-filter.test.js`:

```js
import { test, expect } from "bun:test";
import { computeFeedView } from "./feed-filter.js";

// items：信息流里有序的项；kind 'sep' 是月分隔，'card' 是卡片。
const ITEMS = [
  { kind: "sep", month: "2026-06" },
  { kind: "card", type: "概念", boards: ["算力"] },
  { kind: "card", type: "股票", boards: ["算力", "机器人"] },
  { kind: "sep", month: "2026-05" },
  { kind: "card", type: "人物", boards: [] },
];

test("默认（全部/无板块）：所有卡片可见，所有分隔可见，计数=卡片数", () => {
  const { visible, count } = computeFeedView(ITEMS, { type: "all", board: null });
  expect(count).toBe(3);
  expect(visible).toEqual([true, true, true, true, true]);
});

test("按类型筛选：只留该类型卡片", () => {
  const { visible, count } = computeFeedView(ITEMS, { type: "股票", board: null });
  expect(count).toBe(1);
  expect(visible[2]).toBe(true);
  expect(visible[1]).toBe(false);
  expect(visible[4]).toBe(false);
});

test("按板块筛选：只留含该板块的卡片", () => {
  const { visible, count } = computeFeedView(ITEMS, { type: "all", board: "机器人" });
  expect(count).toBe(1);
  expect(visible[2]).toBe(true);
});

test("类型 + 板块叠加（AND）", () => {
  const { count } = computeFeedView(ITEMS, { type: "概念", board: "机器人" });
  expect(count).toBe(0); // 概念那条没有机器人板块
  const r2 = computeFeedView(ITEMS, { type: "股票", board: "机器人" });
  expect(r2.count).toBe(1);
});

test("没有可见卡片的月分隔要隐藏", () => {
  // 只剩五月那条人物可见 → 六月分隔(索引0)隐藏，五月分隔(索引3)可见
  const { visible } = computeFeedView(ITEMS, { type: "人物", board: null });
  expect(visible[0]).toBe(false);
  expect(visible[3]).toBe(true);
  expect(visible[4]).toBe(true);
});

test("全空时所有分隔都隐藏，计数=0", () => {
  const { visible, count } = computeFeedView(ITEMS, { type: "事件", board: null });
  expect(count).toBe(0);
  expect(visible).toEqual([false, false, false, false, false]);
});
```

- [ ] **Step 2: 跑测试，确认失败**

Run: `bun test web/src/assets/feed-filter.test.js`
Expected: FAIL（`feed-filter.js` 不存在）

- [ ] **Step 3: 实现纯函数**

Create `web/src/assets/feed-filter.js`:

```js
// 首页信息流的可见性 + 计数纯函数。DOM 层（feed.js）只做映射与套用，逻辑全在此处便于单测。
// items：有序数组，元素 { kind:'card'|'sep', type?, boards?:string[] }。
// filters：{ type:'all'|<类型>, board:null|<板块名> }。
// 返回 { visible:boolean[], count:number }，visible 与 items 一一对应；count 为可见卡片数。
export function computeFeedView(items, { type = "all", board = null } = {}) {
  const visible = new Array(items.length).fill(false);
  let count = 0;

  for (let i = 0; i < items.length; i++) {
    const it = items[i];
    if (it.kind !== "card") continue;
    const okType = type === "all" || it.type === type;
    const okBoard = !board || (it.boards || []).includes(board);
    visible[i] = okType && okBoard;
    if (visible[i]) count++;
  }

  // 月分隔：本段（到下一个 sep 之前）有任一可见卡片才显示。
  for (let i = 0; i < items.length; i++) {
    if (items[i].kind !== "sep") continue;
    let any = false;
    for (let j = i + 1; j < items.length && items[j].kind !== "sep"; j++) {
      if (visible[j]) { any = true; break; }
    }
    visible[i] = any;
  }

  return { visible, count };
}
```

- [ ] **Step 4: 跑测试，确认通过**

Run: `bun test web/src/assets/feed-filter.test.js`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add web/src/assets/feed-filter.js web/src/assets/feed-filter.test.js
git commit -m "feat(web): 首页筛选/计数/月分隔可见性纯函数 + 单测

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: 首页模板 + 样式 + 前端逻辑接线

视觉/DOM 改动为主，验证靠 `bun test` 全绿 + `bun run build` 成功 + 本地预览人工核对。

**Files:**
- Modify: `web/src/index.template.html`
- Modify: `web/src/assets/feed.css`
- Modify: `web/src/assets/feed.js`

- [ ] **Step 1: 模板加板块 chips、计数、吸顶筛选区**

In `web/src/index.template.html`, replace the block from `<div class="search-wrap">…` through `<div class="rule-grad"></div>` (currently lines 20–29) with:

```html
      <div class="filterbar">
        <div class="search-wrap"><input id="q" class="search" placeholder="搜索题目、结论、标签…" autocomplete="off"></div>
        <div class="chips" id="chips-type" data-group="type">
          <span class="chip on" data-filter="all">全部</span>
          <span class="chip" data-filter="type:概念">概念</span>
          <span class="chip" data-filter="type:人物">人物</span>
          <span class="chip" data-filter="type:方法论">方法论</span>
          <span class="chip" data-filter="type:事件">事件</span>
          <span class="chip" data-filter="type:股票">股票</span>
        </div>
        <div class="chips chips-board" id="chips-board" data-group="board">
          <span class="blabel">板块</span>
          <span class="chip bd" data-filter="board:光模块">光模块</span>
          <span class="chip bd" data-filter="board:机器人">机器人</span>
          <span class="chip bd" data-filter="board:算力">算力</span>
          <span class="chip bd" data-filter="board:AI应用">AI应用</span>
          <span class="chip bd" data-filter="board:航天">航天</span>
        </div>
        <div class="count" id="count" aria-live="polite"></div>
      </div>
```

（说明：板块 chips 文案须与 `web/build/boards.js` 的 `BOARDS` 一一对应。）

- [ ] **Step 2: feed.css —— `:root` 加 `--measure`，替换卡片/加吸顶/月分隔/计数/按钮贴列**

In `web/src/assets/feed.css`:

(a) 给亮色与深色两个 `:root` 各加一行 `--measure:46rem;`（与 `.wrap` 的 max-width 对齐）。

(b) 删除 `bindTilt` 相关的 3D 倾斜规则并替换卡片样式。Replace the rules for `.article-list`、`@keyframes riseIn`、`.article-card*`、`.card-link*`、`.card-top`、`.ctype`、`.cdate`、`.card-title`、`.lead`、`.card-foot`、`.srcs`（feed.css 第 38–68 行那一段）with:

```css
.article-list{list-style:none; margin-top:.4rem}
@keyframes riseIn{from{opacity:0; transform:translateY(12px)} to{opacity:1; transform:none}}
.article-card{animation:riseIn .5s cubic-bezier(.2,.7,.2,1) backwards}
.article-card:nth-child(-n+6){animation-delay:calc(var(--i,0) * .04s)}
.article-card.hide{display:none}
.month-sep{font-family:var(--sans); font-size:.7rem; letter-spacing:.14em; text-transform:uppercase;
  color:var(--muted); padding:1.1rem .55rem .35rem}
.month-sep.hide{display:none}
.card-link{position:relative; display:flex; gap:.7rem; align-items:flex-start; text-decoration:none; color:inherit;
  padding:.7rem .55rem .75rem; border-radius:9px; transition:background .18s ease}
.card-link::before{content:""; position:absolute; left:0; top:.65rem; bottom:.65rem; width:3px; border-radius:3px;
  background:var(--seal); transform:scaleX(0); transform-origin:left; opacity:0; transition:transform .22s ease, opacity .22s ease}
.card-link:hover{background:var(--card)}
.card-link:hover::before{transform:scaleX(1); opacity:.9}
.article-card:not(:last-child) .card-link{box-shadow:inset 0 -1px 0 var(--hair)}
.ctype{flex:none; margin-top:.18rem; font-family:var(--sans); font-size:.6rem; font-weight:600; letter-spacing:.05em;
  color:var(--seal); border:1px solid var(--rule); border-radius:5px; padding:.1rem .42rem}
.card-body{min-width:0; flex:1}
.card-title{font-family:var(--serif); font-size:1.12rem; line-height:1.3; font-weight:600; margin:0;
  white-space:nowrap; overflow:hidden; text-overflow:ellipsis; transition:color .2s ease}
.card-link:hover .card-title{color:var(--seal)}
.lead{color:var(--ink-soft); font-size:.86rem; line-height:1.45; margin:.16rem 0 0;
  white-space:nowrap; overflow:hidden; text-overflow:ellipsis}
.card-meta{font-family:var(--sans); font-size:.7rem; color:var(--muted); margin-top:.32rem}
```

(c) 加吸顶筛选区 + 板块行 + 计数样式（接在 `.chips`/`.chip` 规则后）：

```css
.filterbar{position:sticky; top:0; z-index:30; background:var(--paper);
  margin:0 -1.5rem; padding:.7rem 1.5rem .55rem; border-bottom:1px solid transparent; transition:border-color .2s}
.filterbar.stuck{border-bottom-color:var(--rule)}
.chips-board{margin-top:.4rem}
.blabel{align-self:center; font-family:var(--sans); font-size:.7rem; color:var(--muted); margin-right:.1rem}
.chip.bd{border-style:dashed}
.count{font-family:var(--sans); font-size:.72rem; color:var(--muted); margin-top:.55rem}
```

(d) 浮动按钮贴正文列：把 `.to-top, .to-refresh{… right:20px …}` 里的 `right:20px` 改成

```css
  right:max(20px, calc((100vw - var(--measure)) / 2 - 56px));
```

（`.to-refresh{bottom:20px}` 与 `.to-top{bottom:74px …}` 不变。）

- [ ] **Step 3: feed.js —— 两组筛选 + 计数 + 月分隔联动 + 搜索切换；移除 tilt**

In `web/src/assets/feed.js`:

(a) 顶部 import 处加：`import { computeFeedView } from "./feed-filter.js";`

(b) 删除整个 `bindTilt()` 函数定义，并删除文件底部对 `bindTilt();` 的调用。

(c) 用下面这版替换整个 `bindChips()` 函数：

```js
// 两组独立筛选（类型 / 板块），AND 组合；联动月分隔可见性与计数。
function bindChips(){
  const typeChips = document.getElementById("chips-type");
  const boardChips = document.getElementById("chips-board");
  const feed = document.getElementById("feed");
  const empty = document.getElementById("empty");
  const countEl = document.getElementById("count");
  const nodes = [...feed.children]; // li.article-card / li.month-sep（有序）
  const items = nodes.map((n) =>
    n.classList.contains("month-sep")
      ? { kind: "sep" }
      : { kind: "card", type: n.dataset.type || "", boards: (n.dataset.boards || "").split(",").filter(Boolean) }
  );
  let activeType = "all";
  let activeBoard = null;

  function apply(){
    const { visible, count } = computeFeedView(items, { type: activeType, board: activeBoard });
    nodes.forEach((n, i) => n.classList.toggle("hide", !visible[i]));
    if (countEl) countEl.textContent = `共 ${count} 篇`;
    empty.hidden = count > 0;
  }

  typeChips.addEventListener("click", (e) => {
    const chip = e.target.closest(".chip"); if (!chip) return;
    typeChips.querySelectorAll(".chip").forEach((c) => c.classList.remove("on"));
    chip.classList.add("on");
    const f = chip.dataset.filter;
    activeType = f === "all" ? "all" : f.slice(5); // type:概念 → 概念
    apply();
  });

  boardChips.addEventListener("click", (e) => {
    const chip = e.target.closest(".chip"); if (!chip) return;
    const name = chip.dataset.filter.slice(6); // board:算力 → 算力
    if (activeBoard === name) { activeBoard = null; chip.classList.remove("on"); } // 再点取消
    else {
      boardChips.querySelectorAll(".chip").forEach((c) => c.classList.remove("on"));
      chip.classList.add("on"); activeBoard = name;
    }
    apply();
  });

  apply(); // 初始计数
}
```

(d) `bindSearch()` 里的 `showFeed`/`showResults` 改为切换整条筛选区与计数（替换这两个函数体内对 `chips` 的处理）：

```js
  const filterbar = document.querySelector(".filterbar");
  const typeChips = document.getElementById("chips-type");
  const boardChips = document.getElementById("chips-board");
  const countEl = document.getElementById("count");
  function setChrome(show){
    [typeChips, boardChips, countEl].forEach((el) => { if (el) el.hidden = !show; });
  }
  function showFeed(){ results.hidden = true; results.innerHTML = ""; feed.hidden = false; setChrome(true); }
  function showResults(){ feed.hidden = true; setChrome(false); results.hidden = false; }
```

（注意：`filterbar` 容器本身不隐藏，搜索框始终在；只隐藏 chips/计数。原 `bindSearch` 里取 `chips` 的那行删掉。）

(e) 新增滚动时给吸顶区加底边（接在 `bindToTop` 之后定义并在底部调用）：

```js
function bindStuck(){
  const bar = document.querySelector(".filterbar");
  if (!bar) return;
  const onScroll = () => bar.classList.toggle("stuck", window.scrollY > 4);
  window.addEventListener("scroll", onScroll, { passive: true });
  onScroll();
}
```

底部调用区改为（去掉 `bindTilt()`，加 `bindStuck()`）：

```js
bindToTop();
bindStuck();
bindChips();
bindSearch();
bindSubmitModal();
bindRefresh();
```

- [ ] **Step 4: 全量单测仍绿**

Run: `bun test`
Expected: PASS（含既有 submit/admin/build 等用例；新逻辑在 feed-filter 已覆盖）

- [ ] **Step 5: 构建并本地预览核对**

Run: `bun run build`
Expected: 构建成功，`web/dist/index.html` 生成。

然后用 preview 工具起服务核对（手机宽度 + 电脑宽度 + 深色）：一屏卡片更多；类型/板块两行可叠加筛选；月分隔出现且筛选后空月隐藏；计数随筛选更新；筛选区吸顶；浮动按钮在宽屏贴正文列右缘。

- [ ] **Step 6: 提交**

```bash
git add web/src/index.template.html web/src/assets/feed.css web/src/assets/feed.js
git commit -m "feat(web): 首页紧凑卡片 + 板块筛选 + 计数 + 按月分组 + 吸顶 + 按钮贴列

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: 详情页阅读进度条 + 浮动按钮贴正文列

**Files:**
- Modify: `web/build/inject-report-nav.js`
- Test: `web/build/inject-report-nav.test.js`

- [ ] **Step 1: 加进度条 + 按钮贴列测试**

Append to `web/build/inject-report-nav.test.js`:

```js
test("注入顶部阅读进度条", () => {
  const out = injectReportNav(BASE);
  expect(out).toContain('class="sx-progress"');
  expect(out).toContain('aria-hidden="true"');
});

test("浮动按钮贴正文列：right 用 --measure 计算而非死贴 20px", () => {
  const out = injectReportNav(BASE);
  expect(out).toContain("(100vw - var(--measure)) / 2 - 56px");
});

test("进度条脚本按文档滚动比例更新宽度", () => {
  const out = injectReportNav(BASE);
  expect(out).toContain("scrollHeight");
  expect(out).toContain("sx-progress");
});
```

- [ ] **Step 2: 跑测试，确认失败**

Run: `bun test web/build/inject-report-nav.test.js`
Expected: FAIL（尚无 `sx-progress`）

- [ ] **Step 3: 在 inject-report-nav.js 注入进度条 + 改按钮定位**

In `web/build/inject-report-nav.js`, inside the injected `<style>` block, add these rules (放在 `.sx-nav-btn{…}` 规则之前)：

```css
.sx-progress{position:fixed; top:0; left:0; right:0; height:3px; z-index:60; background:transparent}
.sx-progress>i{display:block; height:100%; width:0; background:var(--seal); transition:width .1s linear}
@media (prefers-reduced-motion: reduce){ .sx-progress>i{transition:none} }
```

把 `.sx-nav-btn{… right:20px …}` 里的 `right:20px` 改成：

```css
  right:max(20px, calc((100vw - var(--measure)) / 2 - 56px));
```

（报告模板 `:root` 已有 `--measure:42rem`，注入 CSS 直接复用。）

In the `snippet` 的 HTML 部分，在 `<a class="sx-nav-btn sx-home" …>` 之前插入进度条节点：

```html
<div class="sx-progress" aria-hidden="true"><i></i></div>
```

把 snippet 末尾的 `<script>(function(){ … })();</script>` 替换为（在原回顶逻辑基础上加进度条更新）：

```html
<script>
(function(){
  var reduce = window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
  var top = document.querySelector(".sx-top");
  var bar = document.querySelector(".sx-progress > i");
  function onScroll(){
    (window.scrollY > 420) ? top.classList.add("show") : top.classList.remove("show");
    if (bar){
      var h = document.documentElement.scrollHeight - window.innerHeight;
      bar.style.width = (h > 0 ? (window.scrollY / h) * 100 : 0) + "%";
    }
  }
  window.addEventListener("scroll", onScroll, { passive:true });
  onScroll();
  top.addEventListener("click", function(){ window.scrollTo({ top:0, behavior: reduce ? "auto" : "smooth" }); });
})();
</script>
```

- [ ] **Step 4: 跑测试，确认通过**

Run: `bun test web/build/inject-report-nav.test.js`
Expected: PASS（含原有断言不破）

- [ ] **Step 5: 提交**

```bash
git add web/build/inject-report-nav.js web/build/inject-report-nav.test.js
git commit -m "feat(web): 报告页注入阅读进度条 + 浮动按钮贴正文列

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: 详情页自动目录（电脑侧栏 + 手机浮层 + 目录按钮）

**Files:**
- Modify: `web/build/inject-report-nav.js`
- Test: `web/build/inject-report-nav.test.js`

- [ ] **Step 1: 加目录注入测试**

Append to `web/build/inject-report-nav.test.js`:

```js
test("注入目录骨架：电脑侧栏 + 手机浮层 + 目录按钮", () => {
  const out = injectReportNav(BASE);
  expect(out).toContain('class="sx-toc"');            // 电脑侧栏容器
  expect(out).toContain('class="sx-nav-btn sx-toc-btn"'); // 手机目录按钮
  expect(out).toContain('class="sx-toc-sheet"');      // 手机浮层
  expect(out).toContain('aria-label="目录"');
});

test("目录脚本按既定区块顺序扫描并支持点击跳转", () => {
  const out = injectReportNav(BASE);
  expect(out).toContain("核心结论");
  expect(out).toContain("关键发现");
  expect(out).toContain("来源清单");
  expect(out).toContain("main h2");                   // 正文章节来自 main 下的 h2
});

test("窄屏阈值：电脑侧栏在窄屏隐藏，目录按钮在宽屏隐藏", () => {
  const out = injectReportNav(BASE);
  expect(out).toContain("@media (min-width:1100px)");
});
```

- [ ] **Step 2: 跑测试，确认失败**

Run: `bun test web/build/inject-report-nav.test.js`
Expected: FAIL（尚无目录节点）

- [ ] **Step 3: 注入目录 CSS + 节点 + 脚本**

In `web/build/inject-report-nav.js` 的 `<style>` 块追加：

```css
.sx-toc{position:fixed; top:0; bottom:0; display:none; flex-direction:column; justify-content:center;
  width:170px; left:max(20px, calc((100vw - var(--measure)) / 2 - 190px)); z-index:40; pointer-events:none}
.sx-toc nav{pointer-events:auto; max-height:74vh; overflow-y:auto}
.sx-toc .h{font-family:ui-sans-serif,-apple-system,"PingFang SC",sans-serif; font-size:.66rem; letter-spacing:.16em;
  text-transform:uppercase; color:var(--muted); margin-bottom:.6rem}
.sx-toc a{display:block; font-family:ui-sans-serif,-apple-system,"PingFang SC",sans-serif; font-size:.8rem;
  line-height:1.35; color:var(--ink-soft); padding:.32rem 0 .32rem .6rem; border-left:2px solid transparent;
  text-decoration:none; cursor:pointer; transition:color .15s, border-color .15s}
.sx-toc a:hover{color:var(--seal)}
.sx-toc a.on{color:var(--seal); border-left-color:var(--seal); font-weight:600}
.sx-toc-sheet{position:fixed; inset:0; z-index:70; display:none; background:rgba(20,16,10,.42)}
.sx-toc-sheet.open{display:block}
.sx-toc-sheet .panel{position:absolute; left:0; right:0; bottom:0; max-height:70vh; overflow-y:auto;
  background:var(--paper); border-top:1px solid var(--rule); border-radius:16px 16px 0 0; padding:1rem 1.2rem 1.4rem}
.sx-toc-sheet .grip{width:34px; height:4px; border-radius:2px; background:var(--rule); margin:0 auto .8rem}
.sx-toc-sheet a{display:block; font-family:ui-sans-serif,-apple-system,"PingFang SC",sans-serif; font-size:.95rem;
  color:var(--ink-soft); padding:.6rem 0; border-bottom:1px solid var(--rule); text-decoration:none}
.sx-toc-sheet a.on{color:var(--seal); font-weight:600}
.sx-toc-btn{bottom:128px}
@media (min-width:1100px){ .sx-toc{display:flex} .sx-toc-btn{display:none} }
@media (prefers-reduced-motion: reduce){ .sx-toc a{transition:none} }
```

在 snippet 的 HTML 部分（进度条之后、`sx-home` 之前）插入目录骨架：

```html
<aside class="sx-toc" aria-label="目录"><nav><div class="h">目录</div></nav></aside>
<button type="button" class="sx-nav-btn sx-toc-btn" aria-label="目录" title="目录"><i></i>≡</button>
<div class="sx-toc-sheet"><div class="panel"><div class="grip"></div></div></div>
```

把目录构建逻辑加进 snippet 末尾的 `<script>`（在 IIFE 内、回顶逻辑之后）：

```js
  // ── 自动目录：按固定区块 + 正文 h2 顺序扫描 ──
  var secs = [];
  function add(el, label){
    if (!el) return;
    if (!el.id) el.id = "sx-sec-" + secs.length;
    secs.push({ id: el.id, label: label });
  }
  add(document.querySelector(".tldr"), "核心结论");
  add(document.querySelector(".findings"), "关键发现");
  document.querySelectorAll("main h2").forEach(function(h){ add(h, h.textContent.trim()); });
  add(document.querySelector("section.risks"), "风险与争议");
  add(document.querySelector(".glossary"), "名词小抄");
  add(document.querySelector("section.sources"), "来源清单");

  function linksHtml(){
    return secs.map(function(s){
      return '<a href="#" data-id="' + s.id + '">' + s.label + '</a>';
    }).join("");
  }
  var deskNav = document.querySelector(".sx-toc nav");
  var sheet = document.querySelector(".sx-toc-sheet");
  var sheetPanel = sheet.querySelector(".panel");
  if (secs.length){
    deskNav.insertAdjacentHTML("beforeend", linksHtml());
    sheetPanel.insertAdjacentHTML("beforeend", linksHtml());
  }
  function jump(id){
    var t = document.getElementById(id);
    if (t) t.scrollIntoView({ behavior: reduce ? "auto" : "smooth", block: "start" });
  }
  document.querySelectorAll(".sx-toc a, .sx-toc-sheet a").forEach(function(a){
    a.addEventListener("click", function(e){
      e.preventDefault(); jump(a.dataset.id); sheet.classList.remove("open");
    });
  });
  var tocBtn = document.querySelector(".sx-toc-btn");
  tocBtn.addEventListener("click", function(){ sheet.classList.add("open"); });
  sheet.addEventListener("click", function(e){ if (e.target === sheet) sheet.classList.remove("open"); });
  document.addEventListener("keydown", function(e){ if (e.key === "Escape") sheet.classList.remove("open"); });

  // 滚动高亮当前章节
  function spy(){
    var y = window.scrollY + 120, cur = secs.length ? secs[0].id : null;
    for (var i = 0; i < secs.length; i++){
      var el = document.getElementById(secs[i].id);
      if (el && el.getBoundingClientRect().top + window.scrollY <= y) cur = secs[i].id;
    }
    document.querySelectorAll(".sx-toc a, .sx-toc-sheet a").forEach(function(a){
      a.classList.toggle("on", a.dataset.id === cur);
    });
  }
  window.addEventListener("scroll", spy, { passive:true });
  spy();
```

（注意：把这段并入既有 IIFE，复用其中已声明的 `reduce`。）

- [ ] **Step 4: 跑测试，确认通过**

Run: `bun test web/build/inject-report-nav.test.js`
Expected: PASS

- [ ] **Step 5: 构建并预览核对**

Run: `bun run build`
然后预览任一报告页（`web/dist/r/<dir>/index.html`）：电脑宽屏左侧出现目录、当前章节高亮、点击跳转；窄屏左侧目录隐藏、右下「目录」按钮点开浮层可跳转并关闭；进度条随滚动。

- [ ] **Step 6: 提交**

```bash
git add web/build/inject-report-nav.js web/build/inject-report-nav.test.js
git commit -m "feat(web): 报告页注入自动目录（电脑侧栏 + 手机浮层 + 目录按钮）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: skill 板块标签约定 + 存量轻量回填

**Files:**
- Modify: `.claude/skills/research/SKILL.md`
- Modify: `.claude/skills/stock/SKILL.md`
- Modify: 个别 `research/<dir>/notes.md`（仅 frontmatter `related`，确有关联才补）

- [ ] **Step 1: 在两个 SKILL 写明板块约定**

在 `research/SKILL.md` 与 `stock/SKILL.md` 里 frontmatter 说明处加一句明确约定（措辞用朴素中文）：

> `related`：主题确与五大常关注板块（光模块 / 机器人 / 算力 / AI应用 / 航天）之一相关时，把对应板块名写进 `related`（用 `[[板块名]]`）；其余跨主题双链照常放这里。首页板块筛选只认这 5 个名字。

- [ ] **Step 2: 审存量、需要才补板块**

Run（列出 `related` 未命中任一板块的笔记，逐篇判断是否确有关联）：

```bash
cd /Users/yangqiuyuan/Coding/searchX
for f in research/*/notes.md; do grep -qE 'related:.*(光模块|机器人|算力|AI应用|航天)' "$f" || echo "未挂板块: $f"; done
```

对确有关联的，编辑其 `notes.md` 的 `related` 加上对应 `[[板块名]]`；无关联的不动（保持「不硬凑」原则）。

- [ ] **Step 3: 构建确认不破**

Run: `bun run build && bun test`
Expected: 构建成功、测试全绿；改了 `related` 的笔记其卡片微元信息出现对应板块、可被板块筛选命中。

- [ ] **Step 4: 提交**

```bash
git add .claude/skills/research/SKILL.md .claude/skills/stock/SKILL.md research
git commit -m "docs(skill): 写明五大板块标签约定 + 回填存量笔记 related

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 8: 整体验证与收尾

- [ ] **Step 1: 全量测试**

Run: `bun test`
Expected: 全绿。

- [ ] **Step 2: 构建 + 预览全场景核对**

Run: `bun run build`
然后预览核对四象限：
- 手机窄屏：首页一屏 5–6 条、两行筛选可叠加、月分隔、计数、吸顶；报告页右下「目录」浮层 + 进度条；浮动按钮离右缘 20px。
- 电脑宽屏：报告页左侧目录吸顶高亮、浮动按钮贴正文列右缘、左右对称。
- 深色模式：所有新元素配色正常（无写死色）。
- reduced-motion：进度条/平滑滚动/卡片动画降级。

- [ ] **Step 3: 收尾**

调用 superpowers:finishing-a-development-branch，把 `redesign-feed-and-report` 合回 main（合并即触发 CI 部署上线）。合并前向用户确认。

---

## Self-Review（已核对）

- **Spec 覆盖**：紧凑卡片(T1)、全类型板块显示(T1)、板块筛选(T4)、两组 AND(T3/T4)、计数(T3/T4)、按月分组(T2/T4)、吸顶(T4)、进度条(T5)、自动目录(T6)、按钮保留+贴列(T4/T5)、板块约定+回填(T7)、不新增字段(T1 用 boardsOf 交集)、构建注入覆盖存量(T5/T6)。逐条有对应任务。
- **占位符**：无 TBD/TODO；每个代码步给出完整代码。
- **类型一致**：`computeFeedView(items,{type,board})`→`{visible,count}` 在 T3 定义、T4 调用一致；`boardsOf`、`BOARDS` 在 T1 定义、T1/T4(chips 对应) 使用一致；注入新增 class（`sx-progress`/`sx-toc`/`sx-toc-btn`/`sx-toc-sheet`）测试与实现同名。
