# M2b · Runner Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 作者审批（贴 `approved` 标签）后，回到 Mac 跑一条命令 `bun run runner`，它自动取队列里 `approved` 且未 `done` 的 Issue → 喂给本机 Claude Code 跑 `/research`（轻量档，含 Step 6 自动上线）→ 给 Issue 贴 `done` → 取提交者邮箱 → 发一封极简结果邮件（抄送作者）。

**Architecture:** Runner 是**一次性编排脚本**（非常驻），全部副作用（spawn `claude -p`、网络、文件系统、SMTP）集中在薄壳 `index.js`，业务逻辑拆成**纯函数 + 注入依赖**，`bun test` 离线可测（与 M1/M2a 一致）。唯一花 Claude 额度处仍是 `/research` 本身；取 Issue / 贴标签 / 发邮件 / 取邮箱全是确定性脚本、零 token。提交者真实邮箱在 M2a 存于 Worker 私有 KV `sub:<issue号>`，本计划给 Worker 加一个**共享密钥鉴权的只读端点** `GET /sub/<n>` 取出，Cloudflare 凭据不下本机。

**Tech Stack:** bun + `bun:test`（纯函数 + 注入 `fetch`/`transport`/`scanDirs`/`runResearch`，离线可测）；GitHub REST API（作者 fine-grained PAT，仅 searchX Issues:RW）；`Bun.spawn(["claude","-p", …])` 跑 headless `/research`；nodemailer + Gmail SMTP（应用专用密码）；复用 M1 的 `web/build/scan.js`（`scanResearch`）+ `parse-note.js`（`parseNote`，取标题/TLDR/`href`）；扩展 M2a 的 Cloudflare Worker。

---

## 范围与边界

> **🔄 2026-06-03 修正（用户决定）：取消「轻量档」。已移除 `buildResearchPrompt` 的 `[轻量]` 注入与 SKILL 的识别块——所有请求一律走全力档。下文 Task 6 / Task 12 及本节中提到的 `[轻量]` / 轻量档内容均作废，仅作历史记录。**

- **本计划只做 M2b（Runner + Emailer + Worker 只读端点）。** M2a 入队闭环已上线运营，不重做。
- **唯一花 Claude 额度处 = `claude -p "/research …"` 本身**；Runner 编排、取邮箱、发邮件、贴标签全为确定性脚本，零 token。
- **不改 `/research` 既有 6 步产出语义**；只在 SKILL Step 0 **追加**一段「`[轻量]` 标记识别」（默认行为不变，仅当 Runner 注入 `[轻量]` 时收敛规模）。
- **不改 M1 build 输出、不改 M2a 既有 Worker 行为**；Worker 仅**新增**一条 `GET /sub/<n>` 路由（POST 入队路径与测试全部保持绿）。
- 验收（spec §12 M2b / §13 M2）：站上提交 → 作者贴 `approved` → `bun run runner` → 报告自动上线 + Issue 变 `done` + 提交者收到含摘要与链接的邮件；驳回路径 0 花费。

## 已锁定的开放项（2026-06-03 与用户确认）

1. **Runner 驱动 `/research`** = 无头 `claude -p` 全自动（一条命令）。
2. **拉取/改 Issue 的 token** = 新建作者 **fine-grained PAT**（仅 searchX、Issues 读写），与 M2a 的 bot 身份分离。
3. **取提交者邮箱** = 给 Worker 加**共享密钥鉴权的只读端点** `GET /sub/<n>`（Cloudflare 凭据不下本机）。
4. **发信** = Gmail **SMTP 应用专用密码** + nodemailer（纯 bun 脚本、零 token）。

## File Structure

**新建（Worker 侧 · `services/intake-worker/src/`）**
- `sub-read.js` + `.test.js` — `handleSubRead(request, env)`：`GET /sub/<n>`，`x-sub-secret` 头鉴权，读 KV `sub:<n>` 回邮箱。

**修改（Worker 侧）**
- `src/index.js` — 入口加路由：`/sub/` 前缀 → `handleSubRead`，否则 → `handleIntake`。
- `wrangler.toml` — 机密注释加 `SUB_READ_SECRET`。

**新建（Runner 侧 · `services/runner/src/`）**
- `config.js` + `.test.js` — `loadRunnerConfig(env)`：从 `process.env` 读配置，缺必填即抛错列名。
- `issues.js` + `.test.js` — `listApprovedIssues` / `addLabel` / `commentIssue`（注入 fetch）。
- `parse-issue.js` + `.test.js` — `parseIssueRequest({title,body})` → `{topic,focus}`。
- `research-cmd.js` + `.test.js` — `buildResearchPrompt({topic,focus})` → 带 `[轻量]` 的 `/research` 命令串。
- `research-output.js` + `.test.js` — `diffNewDirs(before,after)`：识别本次新产出的主题文件夹。
- `sub-fetch.js` + `.test.js` — `fetchSubmitterEmail({workerUrl,secret,issueNumber})`（注入 fetch）。
- `email.js` + `.test.js` — `composeEmail(...)`（纯函数）+ `sendEmail(message,{transport})`（注入 transport）。
- `runner.js` + `.test.js` — `runOnce(config, deps)`：编排，全部副作用经 deps 注入。
- `index.js` — 一键启动薄壳（装配真实依赖；不单测）。
- `README.md` — 一次性运维 + 运行手册。

**修改（SKILL）**
- `.claude/skills/research/SKILL.md` — Step 0 追加「轻量档识别（`[轻量]` 标记）」一段。

**修改（根）**
- `package.json` — 加 `"runner"` 脚本；`bun add nodemailer`（runtime dep）。
- `.gitignore` — 忽略 `.env` / `.env.local`（Runner 机密只在本机）。

> 命名约定（全计划一致，勿改）：Worker secret `SUB_READ_SECRET`、请求头 `x-sub-secret`、路由 `/sub/<n>`；Runner env：`RUNNER_GITHUB_TOKEN` / `RUNNER_WORKER_URL` / `RUNNER_SUB_SECRET` / `RUNNER_SMTP_USER` / `RUNNER_SMTP_PASS`（必填）+ `RUNNER_OWNER` / `RUNNER_REPO` / `RUNNER_AUTHOR_EMAIL` / `RUNNER_SITE_BASE` / `RUNNER_CLAUDE_ARGS`（可选有默认）；函数名见上；标签机 `pending → approved / rejected → done`。

---

# Phase A — Worker 只读端点 `GET /sub/<n>`（小改 M2a Worker，TDD，离线可测）

> 全部离线 `bun test`；不碰真实 Cloudflare。保持 M2a 既有 POST 入队路径与测试**全绿**。

## Task 1: `handleSubRead` — 密钥鉴权的邮箱只读端点

**Files:**
- Create: `services/intake-worker/src/sub-read.js`
- Test: `services/intake-worker/src/sub-read.test.js`

- [ ] **Step 1: Write the failing test**

```js
// services/intake-worker/src/sub-read.test.js
import { test, expect } from "bun:test";
import { handleSubRead } from "./sub-read.js";

function fakeKV(seed = {}) {
  const m = new Map(Object.entries(seed));
  return { store: m, async get(k){return m.has(k)?m.get(k):null;}, async put(k,v){m.set(k,v);} };
}
const ENV = (over = {}) => ({
  SUB_READ_SECRET: "S3CRET",
  INTAKE_KV: fakeKV({ "sub:7": "alice@gmail.com" }),
  ...over,
});
const get = (path, headers = {}) =>
  new Request(`https://w.dev${path}`, { method: "GET", headers });

test("非 GET → 405", async () => {
  const res = await handleSubRead(new Request("https://w.dev/sub/7", { method: "POST" }), ENV());
  expect(res.status).toBe(405);
});

test("缺密钥头 → 401", async () => {
  expect((await handleSubRead(get("/sub/7"), ENV())).status).toBe(401);
});

test("错密钥 → 401", async () => {
  expect((await handleSubRead(get("/sub/7", { "x-sub-secret": "wrong" }), ENV())).status).toBe(401);
});

test("路径非法（非数字）→ 400", async () => {
  expect((await handleSubRead(get("/sub/abc", { "x-sub-secret": "S3CRET" }), ENV())).status).toBe(400);
});

test("正确密钥 + 命中 → 200 + email", async () => {
  const res = await handleSubRead(get("/sub/7", { "x-sub-secret": "S3CRET" }), ENV());
  expect(res.status).toBe(200);
  expect(await res.json()).toEqual({ ok: true, email: "alice@gmail.com" });
});

test("正确密钥 + 未命中 → 404", async () => {
  expect((await handleSubRead(get("/sub/999", { "x-sub-secret": "S3CRET" }), ENV())).status).toBe(404);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test services/intake-worker/src/sub-read.test.js`
Expected: FAIL — `Cannot find module './sub-read.js'`

- [ ] **Step 3: Write minimal implementation**

```js
// services/intake-worker/src/sub-read.js
// 只读端点：GET /sub/<issue号> → 返回该 Issue 提交者真实邮箱（M2a 存于私有 KV）。
// 仅供作者本机 M2b Runner 用，以共享密钥头鉴权。仓库公开，但此端点与 KV 不公开邮箱（需密钥）。

export async function handleSubRead(request, env) {
  const json = (obj, status = 200) =>
    new Response(JSON.stringify(obj), { status, headers: { "content-type": "application/json" } });

  if (request.method !== "GET") return json({ ok: false, error: "method_not_allowed" }, 405);

  const provided = request.headers.get("x-sub-secret") || "";
  if (!env.SUB_READ_SECRET || provided !== env.SUB_READ_SECRET) {
    return json({ ok: false, error: "unauthorized" }, 401);
  }

  const m = new URL(request.url).pathname.match(/^\/sub\/(\d+)$/);
  if (!m) return json({ ok: false, error: "bad_request" }, 400);

  const email = await env.INTAKE_KV.get(`sub:${m[1]}`);
  if (!email) return json({ ok: false, error: "not_found" }, 404);

  return json({ ok: true, email });
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bun test services/intake-worker/src/sub-read.test.js`
Expected: PASS (6 tests)

- [ ] **Step 5: Commit**

```bash
git add services/intake-worker/src/sub-read.js services/intake-worker/src/sub-read.test.js
git commit -m "feat(worker): add secret-gated GET /sub/<n> email read endpoint (M2b)"
```

## Task 2: Worker 入口加路由 + wrangler 机密注释 + 重打包

**Files:**
- Modify: `services/intake-worker/src/index.js`
- Modify: `services/intake-worker/wrangler.toml`

- [ ] **Step 1: 改入口加路由**

把 `services/intake-worker/src/index.js` 整体替换为：

```js
// services/intake-worker/src/index.js
import { handleIntake } from "./handler.js";
import { handleSubRead } from "./sub-read.js";

export default {
  async fetch(request, env) {
    const { pathname } = new URL(request.url);
    if (pathname.startsWith("/sub/")) return handleSubRead(request, env);
    return handleIntake(request, env);
  },
};
```

- [ ] **Step 2: wrangler.toml 机密注释加 `SUB_READ_SECRET`**

把 `wrangler.toml` 末尾的机密注释那两行改为：

```toml
# 机密不写这里——用 dashboard 或 `bun x wrangler secret put`：
#   TURNSTILE_SECRET, GITHUB_TOKEN, SUB_READ_SECRET
```

- [ ] **Step 3: 全量测试 + 重打包单文件（确认没破 M2a）**

Run:
```bash
bun test services/intake-worker
bun run build:worker
grep -c "export default" services/intake-worker/dist/worker.js
```
Expected: Worker 测试全绿（含新增 6 个 sub-read）；`dist/worker.js` 重新生成且 `export default` ≥ 1。

> 真正部署（设 `SUB_READ_SECRET` + 重新上线 Worker）在 Phase D Task 13；此处只验证代码与打包。

- [ ] **Step 4: Commit**

```bash
git add services/intake-worker/src/index.js services/intake-worker/wrangler.toml
git commit -m "feat(worker): route /sub/<n> to sub-read; note SUB_READ_SECRET (M2b)"
```

---

# Phase B — Runner 纯函数核心（TDD，离线可测，依赖注入）

> 全部 `bun:test` 离线跑通；不碰真实 GitHub/Cloudflare/SMTP/claude。根目录 `bun test` 会递归捡到 `services/runner/**/*.test.js`，与 SKILL Step 6 门禁兼容。

## Task 3: `loadRunnerConfig` — 读配置 / 校验必填

**Files:**
- Create: `services/runner/src/config.js`
- Test: `services/runner/src/config.test.js`

- [ ] **Step 1: Write the failing test**

```js
// services/runner/src/config.test.js
import { test, expect } from "bun:test";
import { loadRunnerConfig } from "./config.js";

const FULL = {
  RUNNER_GITHUB_TOKEN: "ghp_x",
  RUNNER_WORKER_URL: "https://w.workers.dev/",
  RUNNER_SUB_SECRET: "sek",
  RUNNER_SMTP_USER: "me@gmail.com",
  RUNNER_SMTP_PASS: "app-pass",
};

test("齐全 → 返回配置；URL 去尾斜杠；含默认 owner/repo/siteBase/claudeArgs", () => {
  const c = loadRunnerConfig(FULL);
  expect(c.githubToken).toBe("ghp_x");
  expect(c.workerUrl).toBe("https://w.workers.dev");
  expect(c.owner).toBe("qiuyuanqr");
  expect(c.repo).toBe("searchX");
  expect(c.authorEmail).toBe("me@gmail.com"); // 缺省回退到 RUNNER_SMTP_USER
  expect(c.siteBase).toBe("https://qiuyuanqr.github.io/searchX");
  expect(c.claudeArgs).toEqual(["--permission-mode", "bypassPermissions"]);
});

test("缺必填 → 抛错且列出缺的键", () => {
  expect(() => loadRunnerConfig({ ...FULL, RUNNER_SMTP_PASS: "" })).toThrow(/RUNNER_SMTP_PASS/);
});

test("可覆盖默认（siteBase 去尾斜杠、claudeArgs 按空白切分）", () => {
  const c = loadRunnerConfig({
    ...FULL,
    RUNNER_SITE_BASE: "https://x.dev/searchX/",
    RUNNER_CLAUDE_ARGS: "--permission-mode acceptEdits",
  });
  expect(c.siteBase).toBe("https://x.dev/searchX");
  expect(c.claudeArgs).toEqual(["--permission-mode", "acceptEdits"]);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test services/runner/src/config.test.js`
Expected: FAIL — 模块不存在

- [ ] **Step 3: Write minimal implementation**

```js
// services/runner/src/config.js
// 从 process.env 读 Runner 配置；缺必填即抛清晰错误（列出所有缺的键）。
// 机密只在本机环境变量 / 未入库的 .env，绝不入库。

const REQUIRED = [
  "RUNNER_GITHUB_TOKEN", // 作者 fine-grained PAT：仅 searchX、Issues 读写
  "RUNNER_WORKER_URL",   // 形如 https://searchx-intake.qiuyuanqr.workers.dev
  "RUNNER_SUB_SECRET",   // 与 Worker secret SUB_READ_SECRET 同值
  "RUNNER_SMTP_USER",    // Gmail 地址
  "RUNNER_SMTP_PASS",    // Gmail 应用专用密码
];

const trimRight = (u) => String(u).replace(/\/+$/, "");

export function loadRunnerConfig(env) {
  const missing = REQUIRED.filter((k) => !env[k] || !String(env[k]).trim());
  if (missing.length) {
    throw new Error(
      `缺少 Runner 必需环境变量：${missing.join(", ")}（放进未入库的 .env 或 export，绝不入库）`
    );
  }
  return {
    githubToken: env.RUNNER_GITHUB_TOKEN,
    workerUrl: trimRight(env.RUNNER_WORKER_URL),
    subSecret: env.RUNNER_SUB_SECRET,
    smtpUser: env.RUNNER_SMTP_USER,
    smtpPass: env.RUNNER_SMTP_PASS,
    owner: env.RUNNER_OWNER || "qiuyuanqr",
    repo: env.RUNNER_REPO || "searchX",
    authorEmail: env.RUNNER_AUTHOR_EMAIL || env.RUNNER_SMTP_USER,
    siteBase: trimRight(env.RUNNER_SITE_BASE || "https://qiuyuanqr.github.io/searchX"),
    claudeArgs: (env.RUNNER_CLAUDE_ARGS || "--permission-mode bypassPermissions")
      .split(/\s+/)
      .filter(Boolean),
  };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bun test services/runner/src/config.test.js`
Expected: PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add services/runner/src/config.js services/runner/src/config.test.js
git commit -m "feat(runner): loadRunnerConfig — env config + required-key validation (M2b)"
```

## Task 4: GitHub Issues 客户端 — `listApprovedIssues` / `addLabel` / `commentIssue`

**Files:**
- Create: `services/runner/src/issues.js`
- Test: `services/runner/src/issues.test.js`

- [ ] **Step 1: Write the failing test**

```js
// services/runner/src/issues.test.js
import { test, expect } from "bun:test";
import { listApprovedIssues, addLabel, commentIssue } from "./issues.js";

test("listApprovedIssues：过滤掉 PR 与含 done 的，URL/headers 正确", async () => {
  let seen;
  const fetchImpl = async (url, opts) => {
    seen = { url: String(url), opts };
    return {
      ok: true,
      json: async () => [
        { number: 1, title: "A", body: "b1", labels: [{ name: "approved" }] },
        { number: 2, title: "B", body: "b2", labels: [{ name: "approved" }, { name: "done" }] },
        { number: 3, title: "PR", body: "", labels: [{ name: "approved" }], pull_request: {} },
      ],
    };
  };
  const out = await listApprovedIssues({ owner: "o", repo: "r", token: "T" }, fetchImpl);
  expect(out.map((i) => i.number)).toEqual([1]);
  expect(out[0]).toEqual({ number: 1, title: "A", body: "b1", labels: ["approved"] });
  expect(seen.url).toContain("/repos/o/r/issues?state=open&labels=approved");
  expect(seen.opts.headers.authorization).toBe("Bearer T");
  expect(seen.opts.headers["user-agent"]).toBeTruthy();
});

test("listApprovedIssues：非 2xx 抛错", async () => {
  const fetchImpl = async () => ({ ok: false, status: 403 });
  await expect(
    listApprovedIssues({ owner: "o", repo: "r", token: "T" }, fetchImpl)
  ).rejects.toThrow(/403/);
});

test("addLabel POST 到 /labels 带 labels 体", async () => {
  let seen;
  const fetchImpl = async (url, opts) => { seen = { url: String(url), opts }; return { ok: true, json: async () => [] }; };
  await addLabel({ owner: "o", repo: "r", token: "T", number: 5, label: "done" }, fetchImpl);
  expect(seen.url).toBe("https://api.github.com/repos/o/r/issues/5/labels");
  expect(JSON.parse(seen.opts.body)).toEqual({ labels: ["done"] });
});

test("commentIssue POST 到 /comments 带 body", async () => {
  let seen;
  const fetchImpl = async (url, opts) => { seen = { url: String(url), opts }; return { ok: true, json: async () => ({}) }; };
  await commentIssue({ owner: "o", repo: "r", token: "T", number: 5, body: "hi" }, fetchImpl);
  expect(seen.url).toBe("https://api.github.com/repos/o/r/issues/5/comments");
  expect(JSON.parse(seen.opts.body)).toEqual({ body: "hi" });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test services/runner/src/issues.test.js`
Expected: FAIL — 模块不存在

- [ ] **Step 3: Write minimal implementation**

```js
// services/runner/src/issues.js
// GitHub REST 封装（注入 fetch，离线可测）。用作者 fine-grained PAT（Issues 读写）。

const API = "https://api.github.com";
const ghHeaders = (token) => ({
  authorization: `Bearer ${token}`,
  accept: "application/vnd.github+json",
  "content-type": "application/json",
  "user-agent": "searchx-runner",
});

// 取 approved 且未 done 的开放 Issue（排除 PR）。
export async function listApprovedIssues({ owner, repo, token }, fetchImpl = fetch) {
  const url = `${API}/repos/${owner}/${repo}/issues?state=open&labels=approved&per_page=100`;
  const res = await fetchImpl(url, { headers: ghHeaders(token) });
  if (!res.ok) throw new Error(`list issues failed: ${res.status}`);
  const arr = await res.json();
  return arr
    .filter((it) => !it.pull_request)
    .map((it) => ({
      number: it.number,
      title: it.title,
      body: it.body || "",
      labels: (it.labels || []).map((l) => (typeof l === "string" ? l : l.name)),
    }))
    .filter((it) => it.labels.includes("approved") && !it.labels.includes("done"));
}

export async function addLabel({ owner, repo, token, number, label }, fetchImpl = fetch) {
  const url = `${API}/repos/${owner}/${repo}/issues/${number}/labels`;
  const res = await fetchImpl(url, {
    method: "POST",
    headers: ghHeaders(token),
    body: JSON.stringify({ labels: [label] }),
  });
  if (!res.ok) throw new Error(`add label failed: ${res.status}`);
  return true;
}

export async function commentIssue({ owner, repo, token, number, body }, fetchImpl = fetch) {
  const url = `${API}/repos/${owner}/${repo}/issues/${number}/comments`;
  const res = await fetchImpl(url, {
    method: "POST",
    headers: ghHeaders(token),
    body: JSON.stringify({ body }),
  });
  if (!res.ok) throw new Error(`comment failed: ${res.status}`);
  return true;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bun test services/runner/src/issues.test.js`
Expected: PASS (4 tests)

- [ ] **Step 5: Commit**

```bash
git add services/runner/src/issues.js services/runner/src/issues.test.js
git commit -m "feat(runner): GitHub issues client — list approved / add label / comment (M2b)"
```

## Task 5: `parseIssueRequest` — 从 Issue 取对象与侧重点

**Files:**
- Create: `services/runner/src/parse-issue.js`
- Test: `services/runner/src/parse-issue.test.js`

> M2a 的 `formatIssue` 把题目放进 Issue **标题**，把侧重点放进正文 `### 侧重点` 代码围栏。题目直接用标题；侧重点从围栏解析（可能无）。

- [ ] **Step 1: Write the failing test**

```js
// services/runner/src/parse-issue.test.js
import { test, expect } from "bun:test";
import { parseIssueRequest } from "./parse-issue.js";

// 与 M2a issue-format.js 产出的正文一致的夹具
const BODY = [
  "**调研请求**（来自站内表单）",
  "",
  "- 提交者邮箱（打码）：`a****@gmail.com`",
  "- 审批：@qiuyuanqr 贴 `approved` 标签即开始（贴前 0 花费）",
  "",
  "### 题目",
  "```",
  "稳定币的清结算机制",
  "```",
  "",
  "### 侧重点",
  "```",
  "重点讲清算所角色与跨境路径",
  "```",
].join("\n");

test("题目取自标题、侧重点取自围栏", () => {
  const r = parseIssueRequest({ title: "稳定币的清结算机制", body: BODY });
  expect(r.topic).toBe("稳定币的清结算机制");
  expect(r.focus).toBe("重点讲清算所角色与跨境路径");
});

test("无侧重点小节 → focus 为空", () => {
  const body = "### 题目\n```\nX\n```";
  expect(parseIssueRequest({ title: "X", body }).focus).toBe("");
});

test("标题首尾空白被去掉", () => {
  expect(parseIssueRequest({ title: "  CPO  ", body: "" }).topic).toBe("CPO");
});

test("CRLF 行尾（GitHub API 实际返回）也能解析侧重点", () => {
  const crlf = "### 侧重点\r\n```\r\n跨境路径\r\n```";
  expect(parseIssueRequest({ title: "X", body: crlf }).focus).toBe("跨境路径");
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test services/runner/src/parse-issue.test.js`
Expected: FAIL — 模块不存在

- [ ] **Step 3: Write minimal implementation**

```js
// services/runner/src/parse-issue.js
// 从 M2a 建的 Issue 取调研对象与侧重点。
// 题目 = Issue 标题；侧重点 = 正文 `### 侧重点` 代码围栏里的内容（可能无）。

function extractFenced(body, label) {
  // 匹配：### <label>\n```\n<内容>\n```
  const re = new RegExp("###\\s*" + label + "\\s*\\n```\\n([\\s\\S]*?)\\n```");
  const m = body.match(re);
  return m ? m[1].trim() : "";
}

export function parseIssueRequest({ title, body }) {
  // GitHub REST API 的 issue body 常带 \r\n，先归一化再用 \n 正则匹配。
  const normalized = String(body || "").replace(/\r\n/g, "\n");
  return {
    topic: String(title || "").trim(),
    focus: extractFenced(normalized, "侧重点"),
  };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bun test services/runner/src/parse-issue.test.js`
Expected: PASS (4 tests)

- [ ] **Step 5: Commit**

```bash
git add services/runner/src/parse-issue.js services/runner/src/parse-issue.test.js
git commit -m "feat(runner): parseIssueRequest — topic from title, focus from fenced block (M2b)"
```

## Task 6: `buildResearchPrompt` — 拼带「轻量档」标记的 `/research` 命令

**Files:**
- Create: `services/runner/src/research-cmd.js`
- Test: `services/runner/src/research-cmd.test.js`

> 朋友请求默认走轻量档（spec §6.7）：在命令末尾注入 `[轻量]`，SKILL Step 0 据此收敛规模（Phase C 实现）。

- [ ] **Step 1: Write the failing test**

```js
// services/runner/src/research-cmd.test.js
import { test, expect } from "bun:test";
import { buildResearchPrompt } from "./research-cmd.js";

test("有侧重点 → 带 | 与 [轻量]", () => {
  expect(buildResearchPrompt({ topic: "稳定币清结算", focus: "清算所角色" }))
    .toBe("/research 稳定币清结算 | 清算所角色 [轻量]");
});

test("无侧重点 → 仅对象 + [轻量]", () => {
  expect(buildResearchPrompt({ topic: "CPO", focus: "" }))
    .toBe("/research CPO [轻量]");
});

test("首尾空白被去掉", () => {
  expect(buildResearchPrompt({ topic: "  X  ", focus: "  y " }))
    .toBe("/research X | y [轻量]");
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test services/runner/src/research-cmd.test.js`
Expected: FAIL — 模块不存在

- [ ] **Step 3: Write minimal implementation**

```js
// services/runner/src/research-cmd.js
// 拼给本机 Claude Code 跑的 /research 命令。
// 朋友请求默认走「轻量档」：末尾注入 [轻量]，SKILL Step 0 据此收敛检索/规模。

export function buildResearchPrompt({ topic, focus }) {
  const t = String(topic || "").trim();
  const f = String(focus || "").trim();
  const head = f ? `/research ${t} | ${f}` : `/research ${t}`;
  return `${head} [轻量]`;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bun test services/runner/src/research-cmd.test.js`
Expected: PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add services/runner/src/research-cmd.js services/runner/src/research-cmd.test.js
git commit -m "feat(runner): buildResearchPrompt with lightweight [轻量] marker (M2b)"
```

## Task 7: `diffNewDirs` — 识别本次新产出的主题文件夹

**Files:**
- Create: `services/runner/src/research-output.js`
- Test: `services/runner/src/research-output.test.js`

> 跑研究前后各扫一次 `research/`（复用 M1 `scanResearch`），新增的目录即本次产出，作为幂等判据。

- [ ] **Step 1: Write the failing test**

```js
// services/runner/src/research-output.test.js
import { test, expect } from "bun:test";
import { diffNewDirs } from "./research-output.js";

test("返回 after 中新增的目录", () => {
  expect(diffNewDirs(["a", "b"], ["a", "b", "c"])).toEqual(["c"]);
});

test("无新增 → 空数组", () => {
  expect(diffNewDirs(["a"], ["a"])).toEqual([]);
});

test("顺序按 after 保留", () => {
  expect(diffNewDirs([], ["x", "y"])).toEqual(["x", "y"]);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test services/runner/src/research-output.test.js`
Expected: FAIL — 模块不存在

- [ ] **Step 3: Write minimal implementation**

```js
// services/runner/src/research-output.js
// 跑研究前后对比 research/ 目录列表，识别本次新产出的主题文件夹。

export function diffNewDirs(beforeDirs, afterDirs) {
  const before = new Set(beforeDirs);
  return afterDirs.filter((d) => !before.has(d));
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bun test services/runner/src/research-output.test.js`
Expected: PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add services/runner/src/research-output.js services/runner/src/research-output.test.js
git commit -m "feat(runner): diffNewDirs to detect freshly produced research folder (M2b)"
```

## Task 8: `fetchSubmitterEmail` — 经 Worker 只读端点取邮箱

**Files:**
- Create: `services/runner/src/sub-fetch.js`
- Test: `services/runner/src/sub-fetch.test.js`

- [ ] **Step 1: Write the failing test**

```js
// services/runner/src/sub-fetch.test.js
import { test, expect } from "bun:test";
import { fetchSubmitterEmail } from "./sub-fetch.js";

test("带密钥头请求 /sub/<n>，返回 email", async () => {
  let seen;
  const fetchImpl = async (url, opts) => {
    seen = { url: String(url), opts };
    return { ok: true, json: async () => ({ ok: true, email: "a@b.com" }) };
  };
  const email = await fetchSubmitterEmail(
    { workerUrl: "https://w.dev", secret: "S", issueNumber: 7 },
    fetchImpl
  );
  expect(email).toBe("a@b.com");
  expect(seen.url).toBe("https://w.dev/sub/7");
  expect(seen.opts.headers["x-sub-secret"]).toBe("S");
});

test("非 2xx 抛错", async () => {
  const fetchImpl = async () => ({ ok: false, status: 401 });
  await expect(
    fetchSubmitterEmail({ workerUrl: "https://w.dev", secret: "S", issueNumber: 7 }, fetchImpl)
  ).rejects.toThrow(/401/);
});

test("响应缺 email → 抛错", async () => {
  const fetchImpl = async () => ({ ok: true, json: async () => ({ ok: true }) });
  await expect(
    fetchSubmitterEmail({ workerUrl: "https://w.dev", secret: "S", issueNumber: 7 }, fetchImpl)
  ).rejects.toThrow(/email/);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test services/runner/src/sub-fetch.test.js`
Expected: FAIL — 模块不存在

- [ ] **Step 3: Write minimal implementation**

```js
// services/runner/src/sub-fetch.js
// 经 Worker 只读端点取提交者真实邮箱（共享密钥头鉴权）。

export async function fetchSubmitterEmail({ workerUrl, secret, issueNumber }, fetchImpl = fetch) {
  const res = await fetchImpl(`${workerUrl}/sub/${issueNumber}`, {
    headers: { "x-sub-secret": secret },
  });
  if (!res.ok) throw new Error(`fetch submitter email failed: ${res.status}`);
  const data = await res.json();
  if (!data || !data.email) throw new Error("submitter email missing");
  return data.email;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bun test services/runner/src/sub-fetch.test.js`
Expected: PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add services/runner/src/sub-fetch.js services/runner/src/sub-fetch.test.js
git commit -m "feat(runner): fetchSubmitterEmail via worker /sub endpoint (M2b)"
```

## Task 9: `composeEmail` + `sendEmail` — 极简结果邮件

**Files:**
- Create: `services/runner/src/email.js`
- Test: `services/runner/src/email.test.js`

> 极简邮件：一句话摘要 + 网站链接，抄送作者（spec §7）。**隐私红线**：只含报告标题 / 已公开的 TLDR / 公开链接，绝不含任何私人信息。

- [ ] **Step 1: Write the failing test**

```js
// services/runner/src/email.test.js
import { test, expect } from "bun:test";
import { composeEmail, sendEmail } from "./email.js";

test("composeEmail：主题含标题、正文含 TLDR 与链接、抄送作者、from 用 smtpUser", () => {
  const m = composeEmail({
    topic: "稳定币清结算",
    title: "稳定币的清结算机制",
    tldr: "本质是银行间记账",
    url: "https://qiuyuanqr.github.io/searchX/r/2026-06-03_x/",
    toEmail: "u@x.com",
    authorEmail: "me@gmail.com",
    fromEmail: "me@gmail.com",
  });
  expect(m.subject).toContain("稳定币的清结算机制");
  expect(m.text).toContain("本质是银行间记账");
  expect(m.text).toContain("https://qiuyuanqr.github.io/searchX/r/2026-06-03_x/");
  expect(m.to).toBe("u@x.com");
  expect(m.cc).toBe("me@gmail.com");
  expect(m.from).toBe("me@gmail.com");
});

test("无 TLDR 也不报错、不留空噪声行", () => {
  const m = composeEmail({
    topic: "X", title: "X", tldr: "",
    url: "https://x/r/y/", toEmail: "u@x.com", authorEmail: "me@g.com", fromEmail: "me@g.com",
  });
  expect(m.text).toContain("https://x/r/y/");
  expect(m.text).not.toContain("一句话结论：");
});

test("sendEmail 调用注入的 transport.sendMail", async () => {
  let sent;
  const transport = { sendMail: async (msg) => { sent = msg; return { messageId: "1" }; } };
  await sendEmail({ to: "u@x.com" }, { transport });
  expect(sent.to).toBe("u@x.com");
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test services/runner/src/email.test.js`
Expected: FAIL — 模块不存在

- [ ] **Step 3: Write minimal implementation**

```js
// services/runner/src/email.js
// 极简结果邮件：一句话摘要 + 网站链接，抄送作者。纯脚本、零 token。
// 隐私红线：只含报告标题 / 已公开的 TLDR / 公开链接，绝不含任何私人信息。

export function composeEmail({ topic, title, tldr, url, toEmail, authorEmail, fromEmail }) {
  const subject = `【调研完成】${title || topic}`;
  const lines = [
    `你提交的调研「${topic}」已经完成并上线：`,
    "",
    tldr ? `一句话结论：${tldr}` : "",
    "",
    `完整报告：${url}`,
    "",
    "—— searchX 深度调研引擎",
  ].filter((l) => l !== "");
  return { from: fromEmail, to: toEmail, cc: authorEmail, subject, text: lines.join("\n") };
}

// 注入 transport（nodemailer），便于离线单测。
export async function sendEmail(message, { transport }) {
  return transport.sendMail(message);
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bun test services/runner/src/email.test.js`
Expected: PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add services/runner/src/email.js services/runner/src/email.test.js
git commit -m "feat(runner): composeEmail (minimal, privacy-safe) + injectable sendEmail (M2b)"
```

## Task 10: `runOnce` — 编排（取队列 → 跑研究 → 贴 done → 发信 → 留痕）

**Files:**
- Create: `services/runner/src/runner.js`
- Test: `services/runner/src/runner.test.js`

> 全部副作用经 `deps` 注入：`fetchImpl`（github + sub）/`scanDirs`/`runResearch`/`sendEmail`/`log`。其余为纯函数 import。幂等：只处理 `approved` 且未 `done`；研究未产出新文件夹则不贴 `done`，留待重跑。

- [ ] **Step 1: Write the failing test**

```js
// services/runner/src/runner.test.js
import { test, expect } from "bun:test";
import { runOnce } from "./runner.js";

const CONFIG = {
  owner: "o", repo: "r", githubToken: "T",
  workerUrl: "https://w.dev", subSecret: "S",
  authorEmail: "me@g.com", smtpUser: "me@g.com",
  siteBase: "https://site.dev/searchX",
};

const ISSUE_LIST = [
  { number: 7, title: "稳定币清结算", body: "### 侧重点\n```\n清算所\n```", labels: [{ name: "approved" }] },
];

// 路由假 fetch：list / labels / comments / sub
function makeFetch({ subEmail = "u@x.com", subOk = true } = {}) {
  const calls = [];
  const fn = async (url, opts = {}) => {
    calls.push({ url: String(url), opts });
    if (String(url).includes("/issues?")) return { ok: true, json: async () => ISSUE_LIST };
    if (/\/issues\/\d+\/labels$/.test(String(url))) return { ok: true, json: async () => [] };
    if (/\/issues\/\d+\/comments$/.test(String(url))) return { ok: true, json: async () => ({}) };
    if (String(url).includes("/sub/"))
      return subOk
        ? { ok: true, json: async () => ({ ok: true, email: subEmail }) }
        : { ok: false, status: 404 };
    return { ok: false, status: 404, json: async () => ({}) };
  };
  fn.calls = calls;
  return fn;
}

// scanDirs：研究跑完后多出一个新文件夹（带 title/tldr/href，模拟 scanResearch 产出）
function makeWorld() {
  const dirs = [{ dir: "2026-06-01_old", title: "旧", tldr: "t", href: "r/2026-06-01_old/" }];
  return {
    scanDirs: () => dirs.slice(),
    runResearch: async () => {
      dirs.push({
        dir: "2026-06-03_stablecoin",
        title: "稳定币的清结算机制",
        tldr: "银行间记账",
        href: "r/2026-06-03_stablecoin/",
      });
      return true;
    },
  };
}

test("快乐路径：贴 done、发信（含链接+TLDR+抄送）、评论、summary 计数", async () => {
  const fetchImpl = makeFetch();
  const world = makeWorld();
  let sent;
  const summary = await runOnce(CONFIG, {
    fetchImpl, scanDirs: world.scanDirs, runResearch: world.runResearch,
    sendEmail: async (m) => { sent = m; }, log: () => {},
  });
  expect(summary).toEqual({ processed: 1, published: 1, emailed: 1, failed: 0 });
  expect(fetchImpl.calls.some((c) =>
    /\/issues\/7\/labels$/.test(c.url) && JSON.parse(c.opts.body).labels.includes("done")
  )).toBe(true);
  expect(sent.to).toBe("u@x.com");
  expect(sent.cc).toBe("me@g.com");
  expect(sent.text).toContain("https://site.dev/searchX/r/2026-06-03_stablecoin/");
  expect(sent.text).toContain("银行间记账");
});

test("研究未产出新文件夹 → 不贴 done、不发信、failed 计数", async () => {
  const fetchImpl = makeFetch();
  const dirs = [{ dir: "2026-06-01_old", title: "旧", tldr: "t", href: "r/2026-06-01_old/" }];
  let sentCount = 0;
  const summary = await runOnce(CONFIG, {
    fetchImpl, scanDirs: () => dirs.slice(),
    runResearch: async () => true, // 没新增文件夹
    sendEmail: async () => { sentCount++; }, log: () => {},
  });
  expect(summary.published).toBe(0);
  expect(summary.failed).toBe(1);
  expect(sentCount).toBe(0);
  expect(fetchImpl.calls.some((c) => /\/labels$/.test(c.url))).toBe(false);
});

test("发信失败 → 仍贴 done、评论告警、emailed 不计数", async () => {
  const fetchImpl = makeFetch({ subOk: false }); // 取邮箱 404 → 抛错走 catch
  const world = makeWorld();
  const summary = await runOnce(CONFIG, {
    fetchImpl, scanDirs: world.scanDirs, runResearch: world.runResearch,
    sendEmail: async () => {}, log: () => {},
  });
  expect(summary.published).toBe(1);
  expect(summary.emailed).toBe(0);
  expect(fetchImpl.calls.some((c) => /\/issues\/7\/labels$/.test(c.url))).toBe(true);
  expect(fetchImpl.calls.some((c) =>
    /\/issues\/7\/comments$/.test(c.url) && JSON.parse(c.opts.body).body.includes("发信失败")
  )).toBe(true);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test services/runner/src/runner.test.js`
Expected: FAIL — 模块不存在

- [ ] **Step 3: Write minimal implementation**

```js
// services/runner/src/runner.js
// 一次性编排：取 approved 未 done 的 Issue → 跑 /research（轻量档，含 Step6 自动上线）
// → 贴 done → 取提交者邮箱 → 发极简邮件（抄作者）→ 评论留痕。
// 全部副作用经 deps 注入，离线可测。

import { listApprovedIssues, addLabel, commentIssue } from "./issues.js";
import { parseIssueRequest } from "./parse-issue.js";
import { buildResearchPrompt } from "./research-cmd.js";
import { diffNewDirs } from "./research-output.js";
import { fetchSubmitterEmail } from "./sub-fetch.js";
import { composeEmail } from "./email.js";

export async function runOnce(config, deps) {
  const { fetchImpl, scanDirs, runResearch, sendEmail, log } = deps;
  const gh = { owner: config.owner, repo: config.repo, token: config.githubToken };

  const issues = await listApprovedIssues(gh, fetchImpl);
  log(`待处理（approved 未 done）：${issues.length} 条`);

  const summary = { processed: 0, published: 0, emailed: 0, failed: 0 };

  for (const issue of issues) {
    summary.processed++;
    const { topic, focus } = parseIssueRequest(issue);
    log(`#${issue.number} 开始：${topic}`);

    const before = scanDirs().map((e) => e.dir);
    const ok = await runResearch(buildResearchPrompt({ topic, focus }));
    const after = scanDirs();
    const newDirs = diffNewDirs(before, after.map((e) => e.dir));

    if (!ok || newDirs.length === 0) {
      summary.failed++;
      log(`#${issue.number} 研究未产出（claude 退出码非 0 或无新文件夹），不贴 done，留待重跑`);
      continue;
    }

    const entry = after.find((e) => newDirs.includes(e.dir));
    const url = `${config.siteBase}/${entry.href}`;
    await addLabel({ ...gh, number: issue.number, label: "done" }, fetchImpl);
    summary.published++;
    log(`#${issue.number} 已上线：${url}`);

    try {
      const email = await fetchSubmitterEmail(
        { workerUrl: config.workerUrl, secret: config.subSecret, issueNumber: issue.number },
        fetchImpl
      );
      const msg = composeEmail({
        topic, title: entry.title, tldr: entry.tldr, url,
        toEmail: email, authorEmail: config.authorEmail, fromEmail: config.smtpUser,
      });
      await sendEmail(msg);
      summary.emailed++;
      await commentIssue({ ...gh, number: issue.number, body: `✅ 已上线并发信：${url}` }, fetchImpl);
      log(`#${issue.number} 邮件已发送`);
    } catch (err) {
      await commentIssue(
        { ...gh, number: issue.number, body: `⚠️ 报告已上线 ${url}，但发信失败：${err.message}。请手动补发。` },
        fetchImpl
      );
      log(`#${issue.number} 发信失败：${err.message}`);
    }
  }

  log(`完成：处理 ${summary.processed}、上线 ${summary.published}、发信 ${summary.emailed}、失败 ${summary.failed}`);
  return summary;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `bun test services/runner/src/runner.test.js`
Expected: PASS (3 tests)

- [ ] **Step 5: Run the full suite（确认没破 M1/M2a）**

Run: `bun test`
Expected: PASS（M1 10 + M2a 35 + 本计划 Phase A/B 新增的全部）

- [ ] **Step 6: Commit**

```bash
git add services/runner/src/runner.js services/runner/src/runner.test.js
git commit -m "feat(runner): runOnce orchestrator — queue→research→done→email (M2b)"
```

## Task 11: 一键启动薄壳 `index.js` + `package.json` 脚本 + 依赖 + gitignore

**Files:**
- Create: `services/runner/src/index.js`
- Modify: `package.json`
- Modify: `.gitignore`

- [ ] **Step 1: 装 nodemailer（runtime 依赖）**

Run: `bun add nodemailer`
Expected: `package.json` 出现 `"dependencies": { "nodemailer": "…" }`，`node_modules/` 已被 gitignore。

> nodemailer 是纯 JS、Node 兼容，bun 可直接运行；`sendEmail` 经注入 transport，单测不碰它。

- [ ] **Step 2: 写薄壳入口（装配真实依赖，不单测）**

```js
// services/runner/src/index.js
// 一键启动入口：bun run runner。装配真实依赖后跑 runOnce。
// 副作用集中在此（spawn claude / nodemailer / 文件系统 / 网络），不单测——逻辑都在被注入的纯函数里。

import nodemailer from "nodemailer";
import { existsSync } from "fs";
import { scanResearch } from "../../../web/build/scan.js";
import { loadRunnerConfig } from "./config.js";
import { sendEmail as sendEmailImpl } from "./email.js";
import { runOnce } from "./runner.js";

async function main() {
  // 必须在仓库根跑（/research 写 research/、Step 6 的 git push 都依赖当前目录）
  if (!existsSync("research") || !existsSync(".git")) {
    console.error("✗ 请在 searchX 仓库根目录运行（缺 research/ 或 .git/）");
    process.exit(1);
  }
  if (!Bun.which("claude")) {
    console.error("✗ 找不到 claude CLI（headless /research 依赖它）");
    process.exit(1);
  }

  let config;
  try {
    config = loadRunnerConfig(process.env);
  } catch (e) {
    console.error("✗ " + e.message);
    process.exit(1);
  }

  const transport = nodemailer.createTransport({
    host: "smtp.gmail.com",
    port: 465,
    secure: true,
    auth: { user: config.smtpUser, pass: config.smtpPass },
  });

  const summary = await runOnce(config, {
    fetchImpl: fetch,
    scanDirs: () => scanResearch("research"),
    runResearch: async (prompt) => {
      console.log(`→ claude -p ${JSON.stringify(prompt)}`);
      const proc = Bun.spawn(["claude", "-p", prompt, ...config.claudeArgs], {
        stdout: "inherit",
        stderr: "inherit",
        stdin: "inherit",
      });
      const code = await proc.exited;
      return code === 0;
    },
    sendEmail: (msg) => sendEmailImpl(msg, { transport }),
    log: (m) => console.log(m),
  });

  process.exit(summary.failed > 0 ? 1 : 0);
}

main();
```

- [ ] **Step 3: 加 `runner` 脚本到 `package.json`**

在 `scripts` 里加一行（放在 `build:worker` 之后）：

```json
    "runner": "bun run services/runner/src/index.js"
```

- [ ] **Step 4: gitignore Runner 机密**

在 `.gitignore` 的「local / ephemeral」段追加：

```
.env
.env.local
```

- [ ] **Step 5: 冒烟验证（无配置时应给出清晰报错，不崩）**

Run（故意不带任何 RUNNER_* 环境变量）:
```bash
env -u RUNNER_GITHUB_TOKEN bun run runner
```
Expected: 打印 `✗ 缺少 Runner 必需环境变量：RUNNER_GITHUB_TOKEN, …` 并以退出码 1 退出（**不会**真的去 spawn claude / 发请求）。

> 若在仓库根但缺 `.git`/`research` 不会发生；若缺 claude 会先报 claude 缺失。本步只验证“缺配置 → 友好报错”。

- [ ] **Step 6: 全量测试**

Run: `bun test`
Expected: PASS（既有 + 本计划全部；`index.js` 不被测试导入，nodemailer 不影响离线测试）

- [ ] **Step 7: Commit**

```bash
git add services/runner/src/index.js package.json bun.lock .gitignore
git commit -m "feat(runner): one-shot CLI entry (claude -p + nodemailer wiring) + script (M2b)"
```

> 注：bun 锁文件名为 `bun.lock`（或 `bun.lockb`）；`git add` 时按实际存在的那个加。

---

# Phase C — SKILL 轻量档识别（`[轻量]` 标记）

> 仅**追加**，不改既有 6 步语义；只有当 `$ARGUMENTS` 含 `[轻量]`（M2b Runner 注入）时才收敛规模。作者自用不带标记 → 仍走全力档。

## Task 12: SKILL Step 0 追加「轻量档识别」

**Files:**
- Modify: `.claude/skills/research/SKILL.md`

- [ ] **Step 1: 在 Step 0 末尾、关闭 Step 0 的 `---` 之前插入一段**

在 `.claude/skills/research/SKILL.md` 里，定位「语言路由」那段之后、Step 0 结束的 `---` 之前，插入：

```markdown
**轻量档识别（M2b Runner 注入）**：若 `$ARGUMENTS` 含标记 `[轻量]`，先把它从对象名里剥掉得到真正对象，再以**轻量档**执行（朋友通过站点提交、已审批的请求默认走此档以省额度）：
- Step 2 检索收敛到约 **5–8 次**，仅覆盖核心子问题，不铺并行子代理；
- 跳过对抗式交叉验证那一层深挖；人物/方法论的「红旗清单」「激励与反身性」可精简，但 **风险与局限一节必须保留**；
- Step 3 正文聚焦「是什么 / 为什么重要 / 1–2 个真实案例 / 风险与局限 / 下次看什么」，不强求穷尽；
- Step 4 / 5 / 6（三件套、INDEX、自动上线）与全力档 **完全一致，不得省略**——尤其 Step 6 必须照常 push 上线。
作者自用不带该标记时仍走全力档。
```

- [ ] **Step 2: 验证标记已写入**

Run: `grep -c '\[轻量\]' .claude/skills/research/SKILL.md`
Expected: ≥ 1（Runner 的 `buildResearchPrompt` 注入的标记与此处识别规则对齐；真实行为在 Task 14 E2E 验证）。

- [ ] **Step 3: Commit**

```bash
git add .claude/skills/research/SKILL.md
git commit -m "feat(research): recognize [轻量] lightweight profile marker for M2b runner"
```

---

# Phase D — 运维 + 部署 + 端到端验收

> **一次性人工运维**（建 token/密钥、部署 Worker、配本机机密），非 TDD。凭据**永不入库**。`{owner}={qiuyuanqr}`、`{repo}={searchX}`。承接 M2a 的运维事实：Cloudflare 账号 `<你的 Gmail>`、Worker `searchx-intake.qiuyuanqr.workers.dev`、KV `INTAKE_KV`、bot 小号 `qiuyuanmeiguo-ops`。

## Task 13: 配凭据 + 部署带 `/sub` 端点的 Worker + 本机机密

**Files:**
- Modify: `web` / Worker 运行期机密（不入库）；本机 `.env`（不入库）

- [ ] **Step 1: 建作者 fine-grained PAT（仅 searchX、Issues 读写）**
  GitHub → Settings → Developer settings → **Fine-grained tokens** → Generate：
  - Resource owner = `qiuyuanqr`；Repository access = **Only select repositories → searchX**。
  - Permissions → Repository → **Issues: Read and write**（其余 No access）。
  - 复制 token（`github_pat_…`）→ 待写入本机 `.env` 的 `RUNNER_GITHUB_TOKEN`。**绝不入库。**
  > 与 M2a 的 bot classic token 分离：本 token 仅本机 Runner 用，最小权限。贴 `done` 不需通知，身份无所谓。

- [ ] **Step 2: 生成 `/sub` 端点共享密钥**
  Run: `openssl rand -hex 24`
  - 记下输出 = 共享密钥（同时用于 Worker secret `SUB_READ_SECRET` 与本机 `RUNNER_SUB_SECRET`）。

- [ ] **Step 3: 给 Worker 设 `SUB_READ_SECRET` 并部署带 `/sub` 路由的新版本**
  - 重打包：`bun run build:worker`（产出含新路由的 `services/intake-worker/dist/worker.js`）。
  - 设密钥 + 部署（**二选一**；wrangler 在 Claude Code 环境要清掉 agent 环境变量——承接 M2a 经验）：
    - **A · wrangler**：
      ```bash
      cd services/intake-worker
      env -u CLAUDECODE -u AI_AGENT -u CLAUDE_CODE_ENTRYPOINT -u CLAUDE_AGENT_SDK_VERSION bun x wrangler secret put SUB_READ_SECRET   # 粘 Step 2 的密钥
      env -u CLAUDECODE -u AI_AGENT -u CLAUDE_CODE_ENTRYPOINT -u CLAUDE_AGENT_SDK_VERSION bun x wrangler deploy
      ```
    - **B · dashboard**：Workers & Pages → `searchx-intake` → 编辑器粘贴新的 `dist/worker.js` → Settings → Variables and Secrets：加 Secret `SUB_READ_SECRET`（值=Step 2）→ Deploy。
  - 验证端点（用真实存在的 issue 号，如 M2a 的测试 Issue #2，其邮箱在 KV `sub:2`）：
    ```bash
    curl -s -H "x-sub-secret: <密钥>" https://searchx-intake.qiuyuanqr.workers.dev/sub/2
    curl -s -o /dev/null -w "%{http_code}\n" https://searchx-intake.qiuyuanqr.workers.dev/sub/2   # 不带头应回 401
    ```
    Expected: 带正确密钥 → `{"ok":true,"email":"…"}`；不带头 → `401`。
  - 回归确认 POST 入队仍正常（M2a 行为不变）：站上 submit 页或既有 E2E 路径照常工作。

- [ ] **Step 4: 建 Gmail 应用专用密码**
  Google 账号 `<你的 Gmail>` → Security → 确保 **两步验证已开** → **App passwords** → 生成（应用选「邮件」）。
  - 记下 16 位密码 → 待写入 `.env` 的 `RUNNER_SMTP_PASS`；`RUNNER_SMTP_USER` = `<你的 Gmail>`。

- [ ] **Step 5: 确认 headless 自动化权限标志**
  Run: `claude --help | grep -iE "permission|dangerous"`
  - 确认本机 `claude` 支持的“非交互放行”标志（很可能是 `--permission-mode bypassPermissions`，或 `--dangerously-skip-permissions`）。
  - 据此设 `RUNNER_CLAUDE_ARGS`（默认 `--permission-mode bypassPermissions`）。
  > 安全前提：Runner 只跑**已审批**的 Issue；`/research` Step 6 本身有“精准 `git add`（绝不 `-A`）+ 隐私终检”两道闸。放行标志只为让一条命令无人值守跑通；作者自行权衡，写进 README 风险说明。

- [ ] **Step 6: 写本机 `.env`（未入库）**
  在仓库根创建 `.env`（已被 Step 11.4 的 gitignore 覆盖，bun 自动加载）：
  ```
  RUNNER_GITHUB_TOKEN=github_pat_…
  RUNNER_WORKER_URL=https://searchx-intake.qiuyuanqr.workers.dev
  RUNNER_SUB_SECRET=<Step 2 的密钥>
  RUNNER_SMTP_USER=<你的 Gmail>
  RUNNER_SMTP_PASS=<Gmail 应用专用密码>
  # 可选覆盖：RUNNER_CLAUDE_ARGS / RUNNER_SITE_BASE / RUNNER_AUTHOR_EMAIL
  ```
  Run: `git status --porcelain | grep -E '\.env$' && echo "FAIL: .env 被跟踪！" || echo "OK: .env 未被跟踪"`
  Expected: `OK: .env 未被跟踪`。

## Task 14: 端到端验收（M2b「完成」定义）

**Files:**（无代码改动；全链路实跑）

- [ ] **Step 1: 准备一条 `approved` 且有真实邮箱的 Issue**
  - 复用 M2a 的测试 Issue **#2**（作者=bot，邮箱已在 KV `sub:2`）：给它贴 **`approved`** 标签；或在站上 submit 页用**自己的邮箱**新提交一条再贴 `approved`。
  - 确认该 Issue 有 `approved`、无 `done`。

- [ ] **Step 2: 全量测试门禁**
  Run: `bun test`
  Expected: 全绿（M1 10 + M2a 35 + M2b 新增；Phase A/B 全部）。

- [ ] **Step 3: 跑一条命令**
  Run（在仓库根、`main` 分支、工作区干净）: `bun run runner`
  Expected 观察到：
  1. 打印 `待处理（approved 未 done）：N 条` 与 `#<n> 开始：<题目>`；
  2. spawn `claude -p "/research <题目> … [轻量]"`，跑出 `research/<日期>_<slug>/` 三件套，并经 Step 6 `git push`；
  3. Runner 检测到新文件夹 → 给 Issue 贴 `done` + 评论 `✅ 已上线并发信：<url>`；
  4. 提交者邮箱收到「【调研完成】…」邮件（一句话结论 + 链接），抄送作者；
  5. 终端打印 `完成：处理 1、上线 1、发信 1、失败 0`。

- [ ] **Step 4: 线上确认**
  - 打开邮件里的链接 `https://qiuyuanqr.github.io/searchX/r/<日期>_<slug>/`（Pages 构建约 1–2 分钟生效）→ 报告页正常。
  - 首页 `https://qiuyuanqr.github.io/searchX/` 出现该新条目。

- [ ] **Step 5: 幂等性验证**
  Run: `bun run runner`（再跑一次）
  Expected: `待处理（approved 未 done）：0 条`、`完成：处理 0、…`——已 `done` 的不再重复跑（不二次花费、不二次发信）。

- [ ] **Step 6: 驳回路径确认（0 花费）**
  - 另建/选一条仅 `pending`（未 `approved`）的 Issue，关闭它或不动它 → `bun run runner` **不处理**它（0 花费、0 上线）。

- [ ] **Step 7: 写 Runner 运维手册并提交**
  把 Task 13–14 固化为 `services/runner/README.md`（含：① 5 个必填 env + 可选项；② `.env` 永不入库红线；③ Worker `/sub` 端点 + `SUB_READ_SECRET`；④ Gmail 应用专用密码；⑤ headless 放行标志的风险说明与“仅跑已审批 Issue + Step 6 双闸”前提；⑥ 失败重跑/幂等说明；⑦ 发信失败 → Issue 评论告警、可手动补发）。

```bash
git add services/runner/README.md
git commit -m "docs(runner): M2b setup, run & recovery runbook"
```

> 本计划不引入新的发布逻辑：上线复用 M1 的 GitHub Action + `/research` SKILL Step 6 语义（`claude -p` 内部完成 push）。

---

## Self-Review（对照 spec §6.4 / §6.5 / §6.6 / §6.7 / §7 / §10 / §12 / §13）

- **§6.4 本机 Runner「一键启动」`bun run runner`**：取 `approved` 未 `done`（Task 4 `listApprovedIssues` 过滤）→ `claude -p /research`（Task 11 `runResearch` + Task 6 命令拼装）→ 三件套写入（`/research` 既有 Step 4）→ push（Step 6，`claude -p` 内）→ 贴 `done`（Task 4 `addLabel`）→ 触发 Emailer（Task 9/10）✓。可重入/幂等：以 `done` 为标记，研究无产出不贴 done（Task 10）✓。
- **§6.5 自动发布**：不另写发布逻辑，复用 Action + Step 6 ✓。
- **§6.6 安全模型**：唯一花钱动作锁在人工审批后（只跑 `approved`）；Worker `/sub` 端点共享密钥鉴权、Cloudflare 凭据不下本机（Task 1）；作者 PAT 最小权限（Task 13 Step 1）；机密永不入库（`.env` gitignore，Task 11 Step 4 + Task 13 Step 6 校验）✓。
- **§6.7 轻量档 + Token 模型**：Runner 注入 `[轻量]`（Task 6），SKILL 据此收敛（Task 12）；唯一花额度处=`/research` 本身，编排/取邮箱/发信/贴标签全零 token ✓。
- **§7 Emailer**：纯 bun 脚本、零 token；极简邮件（一句话摘要+链接）抄送作者（Task 9）；隐私红线=只含标题/已公开 TLDR/公开链接（Task 9 实现 + 测试）✓。
- **§10.4 Gmail / §10.5 一键启动 / §10.7 轻量档 / §10.8 极简邮件抄作者 / §10.9 零 token**：分别由 Task 13(SMTP)/11/12/9/全程确定性脚本满足 ✓。
- **§12 M2b 验收 / §13 M2 验收**：Task 14 端到端（提交→approved→一条命令→自动上线+收邮件；驳回 0 花费；幂等）✓。

**Placeholder 扫描**：无 TBD/TODO；每个 code step 给完整代码 + 可运行命令与预期。`.env` 里的 `<…>`、`RUNNER_*` 是**运行期真值**（Task 13 显式填的本机机密），非计划占位。

**类型/命名一致性**：Worker secret `SUB_READ_SECRET`、头 `x-sub-secret`、路由 `/sub/<n>` 在 `sub-read.js`/`index.js`/`sub-fetch.js`/wrangler/README 全程一致；Runner env 名在 `config.js`/`.env`/README 一致；函数签名跨定义与调用一致——`listApprovedIssues`/`addLabel`/`commentIssue`(对象参数+fetchImpl)、`parseIssueRequest({title,body})`、`buildResearchPrompt({topic,focus})`、`diffNewDirs(before,after)`、`fetchSubmitterEmail({workerUrl,secret,issueNumber})`、`composeEmail({...})`/`sendEmail(msg,{transport})`、`runOnce(config,deps)`；Runner 复用 M1 `scanResearch`（返回含 `dir`/`title`/`tldr`/`href`）+ URL 拼 `${siteBase}/${href}` = `…/r/<dir>/`，与 build 的 `href: r/${dir}/` 对齐。

**留给后续（spec §11 未来项，本计划不做）**：云端常驻 runner（B 模块）；语义检索；PDF 附件；发信失败的自动重试（当前=Issue 评论告警 + 手动补发，已 `log`，无静默吞错）。
