# 授权用户自助调研、自动放行 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:executing-plans 或 subagent-driven-development 逐任务实现。步骤用 `- [ ]` 勾选跟踪。

**Goal:** 把"逐条手动贴 approved"换成"白名单授权用户用专属链接自助提交、系统自动建 `approved` issue 并由 runner 自动跑"。

**Architecture:** 改动集中在 `services/intake-worker/`（Cloudflare，安全核心）与 `web/`（前端表单 + 新管理页）。Worker 凭链接里的 token 反查邮箱、直接贴 `approved`（命中安全红旗则降级 `pending`）。runner / skill 零改动。所有逻辑纯函数 + 注入依赖，离线可测，沿用现有 bun:test 范式。

**Tech Stack:** Cloudflare Workers（KV `INTAKE_KV`）、bun:test、原生 ESM、纯 HTML/JS 前端（无框架）、`web/build` 自研构建。

**关键依据（现状）：**
- 提交表单是**首页模态框**（`web/src/index.template.html` + `web/src/assets/feed.js`，纯逻辑 `web/src/assets/submit.js`），`submit.template.html` 只是跳转壳。
- 配置用 `{{WORKER_URL}}` / `{{TURNSTILE_SITE_KEY}}` 占位，`web/build/inject-config.js` 注入，`build.js` 产出页面。
- Worker 路由在 `src/index.js`；定长比较 `safeEqual` 现内联在 `src/sub-read.js`；限频键已 `encodeURIComponent` 编码（`src/ratelimit.js`）。
- 测试范式：`fakeKV` + 假 `fetch`，见 `src/handler.test.js`。

**Token / 链接形态：** 个人链接 = `<站点>/?k=<token>`（提交表单是首页模态框，故指向首页带 query）。`?k=` 永不过期，仅撤销/轮换失效。

---

## 文件结构（先锁定边界）

**Worker（`services/intake-worker/src/`）**
| 文件 | 职责 | 新/改 |
|---|---|---|
| `safe-equal.js` | 抽出定长比较 `safeEqual`（DRY：admin + sub-read 共用） | 新 |
| `invite.js` | token 生成 + `invite:`/`allow:` KV 读写 / 列表 / 撤销 / 轮换（纯逻辑，注入 kv/now/gen） | 新 |
| `admin.js` | `/admin/*` 路由：ADMIN_KEY 鉴权 + 失败限流 + 增/查/删/轮换 | 新 |
| `verify.js` | `GET /verify?k=` → 回显打码邮箱或 ok:false | 新 |
| `validate.js` | 去掉 email 必填；改名 `validateContent`（只校验 title/focus/message） | 改 |
| `handler.js` | 提交改造：token 鉴权、去 turnstile/email、红旗分流 approved/pending | 改 |
| `issue-format.js` | `formatIssue` 增 `approved` 分支（label + 正文措辞） | 改 |
| `sub-read.js` | 改用抽出的 `safeEqual`（行为不变） | 改 |
| `index.js` | 路由加 `/verify`、`/admin/` | 改 |
| `wrangler.toml` | 加 vars `MAX_PER_EMAIL_PER_DAY`、`ADMIN_MAX_FAILS_PER_HOUR`；注释 `ADMIN_KEY` secret | 改 |

**前端（`web/`）**
| 文件 | 职责 | 新/改 |
|---|---|---|
| `src/assets/submit.js` | `buildPayload` 改带 `k`、加 `tokenFromQuery`、`describeResult`/`describeVerify` 文案 | 改 |
| `src/assets/feed.js` | 读 `?k=` → verify → 授权态；去 Turnstile；提交带 k | 改 |
| `src/index.template.html` | 模态框去邮箱字段、去 Turnstile（widget + head 脚本）、加授权态条 | 改 |
| `src/admin.template.html` | 管理页（纯密钥闸） | 新 |
| `src/assets/admin.js` | 管理页纯逻辑（拼请求 / 渲染列表 / 拼链接） | 新 |
| `build/build.js` | 产出 `admin.html`（注入 WORKER_URL） | 改 |

---

# 阶段 A · Worker 安全核心

## Task A1: 抽出 `safeEqual` 到共享模块

**Files:**
- Create: `services/intake-worker/src/safe-equal.js`
- Create: `services/intake-worker/src/safe-equal.test.js`
- Modify: `services/intake-worker/src/sub-read.js`（删内联、改 import）

- [ ] **Step 1: 写失败测试** `safe-equal.test.js`

```js
import { test, expect } from "bun:test";
import { safeEqual } from "./safe-equal.js";

test("等长相同 → true", () => expect(safeEqual("abc", "abc")).toBe(true));
test("等长不同 → false", () => expect(safeEqual("abc", "abd")).toBe(false));
test("不等长 → false（不抛）", () => expect(safeEqual("ab", "abc")).toBe(false));
test("非字符串入参被 String 化后比较", () => expect(safeEqual(123, "123")).toBe(true));
test("空串相等", () => expect(safeEqual("", "")).toBe(true));
```

- [ ] **Step 2: 跑测试确认失败** — `bun test services/intake-worker/src/safe-equal.test.js`（Expected: FAIL 模块不存在）

- [ ] **Step 3: 实现** `safe-equal.js`（从 sub-read.js 原样抽出）

```js
// 恒定时间字符串比较：耗时与内容无关，杜绝逐位猜密钥的时序侧信道。等长前提下连长度也不泄露。
export function safeEqual(a, b) {
  a = String(a); b = String(b);
  if (a.length !== b.length) return false;
  let diff = 0;
  for (let i = 0; i < a.length; i++) diff |= a.charCodeAt(i) ^ b.charCodeAt(i);
  return diff === 0;
}
```

- [ ] **Step 4: 改 `sub-read.js`** — 删掉内联 `safeEqual` 函数定义，顶部加 `import { safeEqual } from "./safe-equal.js";`

- [ ] **Step 5: 跑全 Worker 测试确认绿** — `bun test services/intake-worker`（Expected: PASS，sub-read 行为不变）

- [ ] **Step 6: 提交**

```bash
git add services/intake-worker/src/safe-equal.js services/intake-worker/src/safe-equal.test.js services/intake-worker/src/sub-read.js
git commit -m "refactor(intake): 抽出 safeEqual 共享模块（admin 鉴权将复用）"
```

## Task A2: `invite.js` — token 与白名单 KV 读写

**Files:**
- Create: `services/intake-worker/src/invite.js`
- Create: `services/intake-worker/src/invite.test.js`

**接口（后续任务依赖，签名锁定）：**
- `genToken(rand = crypto.getRandomValues)` → 32 位 hex
- `inviteKey(token)` = `` `invite:${token}` ``
- `allowKey(email)` = `` `allow:${encodeURIComponent(email)}` ``
- `async mintInvite(kv, email, { now = Date.now, gen = genToken })` → `{ email, token, addedAt }`（幂等：`allow:` 已存在则复用原 token，不新建）
- `async emailForToken(kv, token)` → `email | null`
- `async listPeople(kv)` → `[{ email, token, addedAt }]`
- `async revoke(kv, email)` → `boolean`
- `async rotate(kv, email, deps)` → `{ email, token } | null`

- [ ] **Step 1: 写失败测试** `invite.test.js`

```js
import { test, expect } from "bun:test";
import { genToken, mintInvite, emailForToken, listPeople, revoke, rotate, inviteKey, allowKey } from "./invite.js";

function fakeKV(seed = {}) {
  const m = new Map(Object.entries(seed));
  return {
    store: m,
    async get(k){ return m.has(k) ? m.get(k) : null; },
    async put(k, v){ m.set(k, v); },
    async delete(k){ m.delete(k); },
    async list({ prefix } = {}){
      const keys = [...m.keys()].filter((k) => !prefix || k.startsWith(prefix)).map((name) => ({ name }));
      return { keys, list_complete: true, cursor: "" };
    },
  };
}
const seq = (...vals) => { let i = 0; return () => vals[i++]; };

test("genToken：32 位十六进制", () => {
  const t = genToken((arr) => { for (let i=0;i<arr.length;i++) arr[i] = i; return arr; });
  expect(t).toMatch(/^[0-9a-f]{32}$/);
});

test("mintInvite：写双向键，返回 token；emailForToken 反查得邮箱", async () => {
  const kv = fakeKV();
  const r = await mintInvite(kv, "bob@x.com", { now: () => 1000, gen: () => "TOK1" });
  expect(r).toEqual({ email: "bob@x.com", token: "TOK1", addedAt: 1000 });
  expect(await kv.get(inviteKey("TOK1"))).toBe("bob@x.com");
  expect(JSON.parse(await kv.get(allowKey("bob@x.com")))).toEqual({ token: "TOK1", addedAt: 1000 });
  expect(await emailForToken(kv, "TOK1")).toBe("bob@x.com");
});

test("mintInvite 幂等：同邮箱再 mint 复用原 token", async () => {
  const kv = fakeKV();
  await mintInvite(kv, "bob@x.com", { now: () => 1, gen: seq("TOK1", "TOK2") });
  const again = await mintInvite(kv, "bob@x.com", { now: () => 2, gen: seq("TOK3") });
  expect(again.token).toBe("TOK1"); // 不新建
});

test("emailForToken：未知 token → null", async () => {
  expect(await emailForToken(fakeKV(), "nope")).toBeNull();
});

test("listPeople：列出全部授权人", async () => {
  const kv = fakeKV();
  await mintInvite(kv, "a@x.com", { now: () => 1, gen: () => "TA" });
  await mintInvite(kv, "b@x.com", { now: () => 2, gen: () => "TB" });
  const people = await listPeople(kv);
  expect(people).toEqual(expect.arrayContaining([
    { email: "a@x.com", token: "TA", addedAt: 1 },
    { email: "b@x.com", token: "TB", addedAt: 2 },
  ]));
  expect(people.length).toBe(2);
});

test("revoke：删双向键，token 失效；不存在返回 false", async () => {
  const kv = fakeKV();
  await mintInvite(kv, "a@x.com", { now: () => 1, gen: () => "TA" });
  expect(await revoke(kv, "a@x.com")).toBe(true);
  expect(await emailForToken(kv, "TA")).toBeNull();
  expect(await kv.get(allowKey("a@x.com"))).toBeNull();
  expect(await revoke(kv, "ghost@x.com")).toBe(false);
});

test("rotate：旧 token 失效、发新 token、邮箱不变", async () => {
  const kv = fakeKV();
  await mintInvite(kv, "a@x.com", { now: () => 1, gen: () => "OLD" });
  const r = await rotate(kv, "a@x.com", { now: () => 9, gen: () => "NEW" });
  expect(r).toEqual({ email: "a@x.com", token: "NEW", addedAt: 9 });
  expect(await emailForToken(kv, "OLD")).toBeNull();
  expect(await emailForToken(kv, "NEW")).toBe("a@x.com");
});

test("含 : 的邮箱不污染键", async () => {
  const kv = fakeKV();
  await mintInvite(kv, "wei:rd@x.com", { now: () => 1, gen: () => "T" });
  expect(await kv.get(allowKey("wei:rd@x.com"))).toBeTruthy();
});
```

- [ ] **Step 2: 跑测试确认失败** — `bun test services/intake-worker/src/invite.test.js`

- [ ] **Step 3: 实现** `invite.js`

```js
// token 生成 + 白名单 KV 读写。纯逻辑，注入 kv / now / gen，离线可测。
// 双向键：invite:<token>→email（提交时 O(1) 反查）、allow:<encEmail>→{token,addedAt}（列表/撤销）。
// 永不过期：put 不带 TTL；唯一失效途径是 revoke / rotate。
export function genToken(rand = (arr) => crypto.getRandomValues(arr)) {
  const a = new Uint8Array(16);
  rand(a);
  return [...a].map((b) => b.toString(16).padStart(2, "0")).join("");
}

export const inviteKey = (token) => `invite:${token}`;
export const allowKey = (email) => `allow:${encodeURIComponent(email)}`;

export async function emailForToken(kv, token) {
  if (!token) return null;
  return (await kv.get(inviteKey(token))) || null;
}

export async function mintInvite(kv, email, { now = Date.now, gen = genToken } = {}) {
  const existing = await kv.get(allowKey(email));
  if (existing) {
    const { token, addedAt } = JSON.parse(existing);
    return { email, token, addedAt };
  }
  const token = gen();
  const addedAt = now();
  await kv.put(inviteKey(token), email);
  await kv.put(allowKey(email), JSON.stringify({ token, addedAt }));
  return { email, token, addedAt };
}

export async function listPeople(kv) {
  const { keys } = await kv.list({ prefix: "allow:" });
  const out = [];
  for (const { name } of keys) {
    const raw = await kv.get(name);
    if (!raw) continue;
    const { token, addedAt } = JSON.parse(raw);
    out.push({ email: decodeURIComponent(name.slice("allow:".length)), token, addedAt });
  }
  return out;
}

export async function revoke(kv, email) {
  const raw = await kv.get(allowKey(email));
  if (!raw) return false;
  const { token } = JSON.parse(raw);
  await kv.delete(inviteKey(token));
  await kv.delete(allowKey(email));
  return true;
}

export async function rotate(kv, email, deps = {}) {
  const existed = await revoke(kv, email);
  if (!existed) return null;
  return mintInvite(kv, email, deps);
}
```

- [ ] **Step 4: 跑测试确认绿** — `bun test services/intake-worker/src/invite.test.js`

- [ ] **Step 5: 提交**

```bash
git add services/intake-worker/src/invite.js services/intake-worker/src/invite.test.js
git commit -m "feat(intake): invite 模块——token 生成 + 白名单 KV 读写（永不过期，可撤销/轮换）"
```

## Task A3: `admin.js` — 管理路由（鉴权 + 失败限流 + 增删查轮换）

**Files:**
- Create: `services/intake-worker/src/admin.js`
- Create: `services/intake-worker/src/admin.test.js`

**鉴权与限流：** 头 `x-admin-key`，`safeEqual` 比 `env.ADMIN_KEY`；按 IP 失败计数键 `afail:<encIp>:<hourKey>`，达 `ADMIN_MAX_FAILS_PER_HOUR`（默认 10）→ 直接 `429`；密钥错 → 失败 +1 后 `401`。CORS 同 intake（`ALLOWED_ORIGIN`）。

- [ ] **Step 1: 写失败测试** `admin.test.js`

```js
import { test, expect } from "bun:test";
import { handleAdmin } from "./admin.js";
import { emailForToken } from "./invite.js";

function fakeKV(seed = {}) {
  const m = new Map(Object.entries(seed));
  return {
    store: m,
    async get(k){ return m.has(k) ? m.get(k) : null; },
    async put(k, v){ m.set(k, v); },
    async delete(k){ m.delete(k); },
    async list({ prefix } = {}){ return { keys: [...m.keys()].filter((k)=>!prefix||k.startsWith(prefix)).map((name)=>({name})), list_complete: true, cursor: "" }; },
  };
}
const ENV = (over = {}) => ({ ALLOWED_ORIGIN: "https://qiuyuanqr.github.io", ADMIN_KEY: "SECRET", INTAKE_KV: fakeKV(), ...over });
const req = (method, path, { key, body } = {}) => new Request(`https://w.dev${path}`, {
  method,
  headers: { "content-type": "application/json", "cf-connecting-ip": "9.9.9.9", ...(key ? { "x-admin-key": key } : {}) },
  body: body ? JSON.stringify(body) : undefined,
});

test("无/错密钥 → 401", async () => {
  expect((await handleAdmin(req("GET", "/admin/list"), ENV())).status).toBe(401);
  expect((await handleAdmin(req("GET", "/admin/list", { key: "WRONG" }), ENV())).status).toBe(401);
});

test("add → 200 返回链接 token；list 能看到；提交侧能反查邮箱", async () => {
  const env = ENV();
  const r = await handleAdmin(req("POST", "/admin/add", { key: "SECRET", body: { email: "bob@x.com" } }), env, { now: () => 5, gen: () => "TOK" });
  expect(r.status).toBe(200);
  const j = await r.json();
  expect(j).toMatchObject({ ok: true, email: "bob@x.com", token: "TOK" });
  expect(await emailForToken(env.INTAKE_KV, "TOK")).toBe("bob@x.com");
  const list = await (await handleAdmin(req("GET", "/admin/list", { key: "SECRET" }), env)).json();
  expect(list.people).toEqual([{ email: "bob@x.com", token: "TOK", addedAt: 5 }]);
});

test("add 非法邮箱 → 400", async () => {
  const r = await handleAdmin(req("POST", "/admin/add", { key: "SECRET", body: { email: "not-an-email" } }), ENV());
  expect(r.status).toBe(400);
});

test("remove → token 失效", async () => {
  const env = ENV();
  await handleAdmin(req("POST", "/admin/add", { key: "SECRET", body: { email: "a@x.com" } }), env, { gen: () => "TA" });
  const r = await handleAdmin(req("POST", "/admin/remove", { key: "SECRET", body: { email: "a@x.com" } }), env);
  expect(r.status).toBe(200);
  expect(await emailForToken(env.INTAKE_KV, "TA")).toBeNull();
});

test("失败限流：超阈值 → 429（即便后来给对密钥也先 429）", async () => {
  const env = ENV({ ADMIN_MAX_FAILS_PER_HOUR: "2" });
  await handleAdmin(req("GET", "/admin/list", { key: "X" }), env, { now: () => 0 }); // fail 1
  await handleAdmin(req("GET", "/admin/list", { key: "X" }), env, { now: () => 0 }); // fail 2
  const r = await handleAdmin(req("GET", "/admin/list", { key: "SECRET" }), env, { now: () => 0 });
  expect(r.status).toBe(429);
});

test("OPTIONS 预检 → 204 + CORS", async () => {
  const r = await handleAdmin(req("OPTIONS", "/admin/list"), ENV());
  expect(r.status).toBe(204);
  expect(r.headers.get("access-control-allow-origin")).toBe("https://qiuyuanqr.github.io");
});

test("未配 ADMIN_KEY → 401（防空密钥裸奔）", async () => {
  const r = await handleAdmin(req("GET", "/admin/list", { key: "" }), ENV({ ADMIN_KEY: "" }));
  expect(r.status).toBe(401);
});
```

- [ ] **Step 2: 跑测试确认失败** — `bun test services/intake-worker/src/admin.test.js`

- [ ] **Step 3: 实现** `admin.js`

```js
import { safeEqual } from "./safe-equal.js";
import { mintInvite, listPeople, revoke, rotate } from "./invite.js";

const isEmail = (s) => /^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(s);
const hourKey = (ms) =>
  new Intl.DateTimeFormat("en-CA", { timeZone: "Asia/Shanghai", year: "numeric", month: "2-digit", day: "2-digit", hour: "2-digit", hour12: false })
    .format(new Date(ms)).replace(/[^0-9]/g, "");

export async function handleAdmin(request, env, deps = {}) {
  const now = deps.now || (() => Date.now());
  const cors = {
    "access-control-allow-origin": env.ALLOWED_ORIGIN,
    "access-control-allow-methods": "GET, POST, OPTIONS",
    "access-control-allow-headers": "content-type, x-admin-key",
    vary: "origin",
  };
  const json = (obj, status = 200) => new Response(JSON.stringify(obj), { status, headers: { "content-type": "application/json", ...cors } });
  if (request.method === "OPTIONS") return new Response(null, { status: 204, headers: cors });

  const kv = env.INTAKE_KV;
  const ip = request.headers.get("cf-connecting-ip") || "0.0.0.0";
  const maxFails = parseInt(env.ADMIN_MAX_FAILS_PER_HOUR || "10", 10) || 10;
  const failKey = `afail:${encodeURIComponent(ip)}:${hourKey(now())}`;

  // 失败限流：先看是否已锁定
  const fails = parseInt((await kv.get(failKey)) || "0", 10);
  if (fails >= maxFails) return json({ ok: false, error: "locked" }, 429);

  // 鉴权：定长比较；空密钥一律拒绝
  const provided = request.headers.get("x-admin-key") || "";
  if (!env.ADMIN_KEY || !safeEqual(provided, env.ADMIN_KEY)) {
    await kv.put(failKey, String(fails + 1), { expirationTtl: 7200 });
    return json({ ok: false, error: "unauthorized" }, 401);
  }

  const { pathname } = new URL(request.url);
  try {
    if (request.method === "GET" && pathname === "/admin/list") {
      return json({ ok: true, people: await listPeople(kv) });
    }
    if (request.method === "POST" && pathname === "/admin/add") {
      const body = await request.json().catch(() => ({}));
      const email = typeof body.email === "string" ? body.email.trim() : "";
      if (!isEmail(email) || email.length > 254) return json({ ok: false, error: "invalid_email" }, 400);
      const r = await mintInvite(kv, email, { now });
      return json({ ok: true, ...r });
    }
    if (request.method === "POST" && pathname === "/admin/remove") {
      const body = await request.json().catch(() => ({}));
      const email = typeof body.email === "string" ? body.email.trim() : "";
      const ok = await revoke(kv, email);
      return json({ ok, removed: ok });
    }
    if (request.method === "POST" && pathname === "/admin/rotate") {
      const body = await request.json().catch(() => ({}));
      const email = typeof body.email === "string" ? body.email.trim() : "";
      const r = await rotate(kv, email, { now });
      return r ? json({ ok: true, ...r }) : json({ ok: false, error: "not_found" }, 404);
    }
    return json({ ok: false, error: "not_found" }, 404);
  } catch {
    return json({ ok: false, error: "internal" }, 500);
  }
}
```

- [ ] **Step 4: 跑测试确认绿** — `bun test services/intake-worker/src/admin.test.js`

- [ ] **Step 5: 提交**

```bash
git add services/intake-worker/src/admin.js services/intake-worker/src/admin.test.js
git commit -m "feat(intake): /admin/* 路由——ADMIN_KEY 鉴权 + 失败限流锁定 + 增/查/删/轮换"
```

## Task A4: `verify.js` — 提交前确认 token

**Files:**
- Create: `services/intake-worker/src/verify.js`
- Create: `services/intake-worker/src/verify.test.js`

- [ ] **Step 1: 写失败测试** `verify.test.js`

```js
import { test, expect } from "bun:test";
import { handleVerify } from "./verify.js";
import { mintInvite } from "./invite.js";

function fakeKV(){ const m=new Map(); return { store:m, async get(k){return m.has(k)?m.get(k):null;}, async put(k,v){m.set(k,v);}, async delete(k){m.delete(k);} }; }
const ENV = (kv) => ({ ALLOWED_ORIGIN: "https://qiuyuanqr.github.io", INTAKE_KV: kv });
const get = (q) => new Request(`https://w.dev/verify${q}`, { method: "GET" });

test("有效 token → ok + 打码邮箱（不泄露完整邮箱）", async () => {
  const kv = fakeKV();
  await mintInvite(kv, "bob@gmail.com", { gen: () => "TOK" });
  const r = await handleVerify(get("?k=TOK"), ENV(kv));
  expect(r.status).toBe(200);
  const j = await r.json();
  expect(j.ok).toBe(true);
  expect(j.email).toBe("b***@gmail.com");
  expect(JSON.stringify(j)).not.toContain("bob@gmail.com");
});

test("无效 token → ok:false", async () => {
  const r = await handleVerify(get("?k=nope"), ENV(fakeKV()));
  expect((await r.json()).ok).toBe(false);
});

test("缺 k → ok:false", async () => {
  const r = await handleVerify(get(""), ENV(fakeKV()));
  expect((await r.json()).ok).toBe(false);
});
```

- [ ] **Step 2: 跑测试确认失败** — `bun test services/intake-worker/src/verify.test.js`

- [ ] **Step 3: 实现** `verify.js`

```js
import { emailForToken } from "./invite.js";
import { maskEmail } from "./issue-format.js";

export async function handleVerify(request, env) {
  const cors = { "access-control-allow-origin": env.ALLOWED_ORIGIN, vary: "origin" };
  const json = (obj, status = 200) => new Response(JSON.stringify(obj), { status, headers: { "content-type": "application/json", ...cors } });
  const token = new URL(request.url).searchParams.get("k") || "";
  const email = await emailForToken(env.INTAKE_KV, token);
  if (!email) return json({ ok: false });
  return json({ ok: true, email: maskEmail(email) });
}
```

- [ ] **Step 4: 跑测试确认绿** — `bun test services/intake-worker/src/verify.test.js`

- [ ] **Step 5: 提交**

```bash
git add services/intake-worker/src/verify.js services/intake-worker/src/verify.test.js
git commit -m "feat(intake): /verify 端点——提交前确认 token 有效并回显打码邮箱"
```

## Task A5: `validate.js` — 去掉 email 必填（改名 `validateContent`）

**Files:**
- Modify: `services/intake-worker/src/validate.js`
- Modify: `services/intake-worker/src/validate.test.js`

- [ ] **Step 1: 改测试** — `validate.test.js` 里把 `validateSubmission` 改 `validateContent`；删与 email 相关断言（`email_required` / `email_invalid`）；保留 title/focus/message 长度与必填、`screenSubmission` 红旗用例不变。新增：

```js
test("validateContent：无 email 字段也通过（email 不再来自表单）", () => {
  const r = validateContent({ title: "稳定币", focus: "机制", message: "" });
  expect(r.ok).toBe(true);
  expect(r.clean).not.toHaveProperty("email");
});
test("title 必填仍生效", () => {
  expect(validateContent({ title: "" }).errors).toContain("title_required");
});
```

- [ ] **Step 2: 跑测试确认失败** — `bun test services/intake-worker/src/validate.test.js`

- [ ] **Step 3: 改实现** `validate.js`：
  - `LIMITS` 删 `email`；
  - 导出改名 `validateContent`（保留旧名做别名 `export const validateSubmission = validateContent;` 以防遗漏调用，最后清理）；
  - 删 email 的取值/校验/`clean.email`；其余（sanitize、screenSubmission、flags）不变。

```js
const LIMITS = { title: 160, focus: 500, message: 1000 };
// ... INJECTION_PATTERNS / screenSubmission / sanitize 不变 ...
export function validateContent(input, limits = LIMITS) {
  const get = (k) => (typeof input?.[k] === "string" ? input[k] : "");
  const title = get("title").trim();
  const focus = get("focus").trim();
  const message = get("message").trim();
  const errors = [];
  if (!title) errors.push("title_required");
  if (title.length > limits.title) errors.push("title_too_long");
  if (focus.length > limits.focus) errors.push("focus_too_long");
  if (message.length > limits.message) errors.push("message_too_long");
  const clean = { title: sanitize(title), focus: sanitize(focus), message: sanitize(message) };
  return { ok: errors.length === 0, errors, clean, flags: screenSubmission(clean) };
}
```

- [ ] **Step 4: 跑测试确认绿** — `bun test services/intake-worker/src/validate.test.js`

- [ ] **Step 5: 提交**

```bash
git add services/intake-worker/src/validate.js services/intake-worker/src/validate.test.js
git commit -m "refactor(intake): validateContent——email 不再来自表单，去其校验"
```

## Task A6: `issue-format.js` — approved/pending 分支

**Files:**
- Modify: `services/intake-worker/src/issue-format.js`
- Modify: `services/intake-worker/src/issue-format.test.js`

- [ ] **Step 1: 改/加测试** — 新增：

```js
test("approved=true：label approved，正文标注自动放行", () => {
  const r = formatIssue({ title: "稳定币", focus: "", message: "", email: "a@x.com" }, { author: "qiuyuanqr", flags: [], approved: true });
  expect(r.labels).toEqual(["approved"]);
  expect(r.body).toContain("自动放行");
});
test("approved=false（命中红旗）：label pending，正文提示人工复核", () => {
  const r = formatIssue({ title: "x", focus: "", message: "", email: "a@x.com" }, { author: "qiuyuanqr", flags: ["代码围栏（```）"], approved: false });
  expect(r.labels).toEqual(["pending"]);
  expect(r.body).toContain("人工复核");
  expect(r.body).toContain("代码围栏");
});
```
（保留既有打码邮箱断言。）

- [ ] **Step 2: 跑测试确认失败** — `bun test services/intake-worker/src/issue-format.test.js`

- [ ] **Step 3: 改实现** `formatIssue`：签名加 `approved`，按它定 label 与来源措辞。

```js
export function formatIssue(clean, { author, flags = [], approved = false }) {
  const maskedEmail = maskEmail(clean.email);
  const lines = [
    "**调研请求**（来自站内表单 · 已授权用户）",
    "",
    `- 提交者邮箱（打码）：\`${maskedEmail}\``,
    approved
      ? "- 状态：✅ 已授权用户，**自动放行**（无需审批，runner 将自动跑）"
      : `- 状态：⚠️ 已授权用户，但命中安全初筛 → **降级人工复核**，@${author} 逐字核对题目/侧重点无误后再贴 \`approved\``,
  ];
  if (flags.length) {
    lines.push("", `> ⚠️ **自动安全初筛**：检测到可疑内容（${flags.join("、")}）。可能是提示注入，**请逐字核对题目与侧重点**。`);
  }
  lines.push(...fence("题目", clean.title));
  if (clean.focus) lines.push(...fence("侧重点", clean.focus));
  if (clean.message) lines.push(...fence("留言", clean.message));
  return { title: clean.title, body: lines.join("\n"), labels: [approved ? "approved" : "pending"], assignees: [author] };
}
```

- [ ] **Step 4: 跑测试确认绿** — `bun test services/intake-worker/src/issue-format.test.js`

- [ ] **Step 5: 提交**

```bash
git add services/intake-worker/src/issue-format.js services/intake-worker/src/issue-format.test.js
git commit -m "feat(intake): issue 正文/标签按 approved/pending 分流"
```

## Task A7: `handler.js` — 提交主流程改造（token 鉴权 / 去 turnstile / 红旗分流）

**Files:**
- Modify: `services/intake-worker/src/handler.js`
- Modify: `services/intake-worker/src/handler.test.js`

- [ ] **Step 1: 重写测试** `handler.test.js`（核心断言）：
  - 入参改 `{ k, title, focus, message }`（无 email/turnstile）；`fakeKV` 加 `delete`/`list`；测试前用 `mintInvite` 预置 token。
  - 用例：
    - 无 token / 未知 token → `403 unauthorized`，不建 issue；
    - 有效 token + 干净内容 → 建 issue（label `approved`）、`sub:<n>` 存映射邮箱、回 ok；GitHub 正文不含完整邮箱、含打码；
    - 有效 token + 命中红旗（如 focus 含 ` ``` `）→ 建 issue（label `pending`）；
    - 超每邮箱每日额度（默认 5）→ 429；
    - 建 issue 失败 → 502 且不计额度；
    - 坏 JSON → 400；
    - 下游抛错 → 结构化 500 带 CORS。

```js
// 片段：预置 token + 快乐路径
const kv = fakeKV();
await mintInvite(kv, "alice@gmail.com", { gen: () => "TOK" });
const env = { ...ENV(), INTAKE_KV: kv };
const res = await handleIntake(post({ k: "TOK", title: "稳定币", focus: "机制", message: "" }), env, { fetch: routeFetch(), now: NOW });
expect(res.status).toBe(200);
const ghCall = ...; expect(JSON.parse(ghCall.opts.body).labels).toEqual(["approved"]);
expect(env.INTAKE_KV.store.get("sub:7")).toBe("alice@gmail.com");
```

- [ ] **Step 2: 跑测试确认失败** — `bun test services/intake-worker/src/handler.test.js`

- [ ] **Step 3: 重写** `handler.js`

```js
import { validateContent } from "./validate.js";
import { peekRateLimit, commitRateLimit, dayKey } from "./ratelimit.js";
import { formatIssue, maskEmail } from "./issue-format.js";
import { createIssue } from "./github.js";
import { emailForToken } from "./invite.js";

export async function handleIntake(request, env, deps = {}) {
  const fetchImpl = deps.fetch || fetch;
  const now = deps.now || new Date();
  const cors = {
    "access-control-allow-origin": env.ALLOWED_ORIGIN,
    "access-control-allow-methods": "POST, OPTIONS",
    "access-control-allow-headers": "content-type",
    vary: "origin",
  };
  const json = (obj, status = 200) => new Response(JSON.stringify(obj), { status, headers: { "content-type": "application/json", ...cors } });
  if (request.method === "OPTIONS") return new Response(null, { status: 204, headers: cors });
  if (request.method !== "POST") return json({ ok: false, error: "method_not_allowed" }, 405);

  let input;
  try { input = await request.json(); } catch { return json({ ok: false, error: "bad_json" }, 400); }

  const ip = request.headers.get("cf-connecting-ip") || "0.0.0.0";
  const token = typeof input?.k === "string" ? input.k : "";

  try {
    // 凭证闸：token 必须有效，邮箱由映射取得（用户不输邮箱 → 杜绝冒充/注入）
    const email = await emailForToken(env.INTAKE_KV, token);
    if (!email) return json({ ok: false, error: "unauthorized" }, 403);

    const { ok: valid, errors, clean, flags } = validateContent(input);
    if (!valid) return json({ ok: false, error: "invalid", details: errors }, 400);

    const emailCap = parseInt(env.MAX_PER_EMAIL_PER_DAY || "5", 10) || 5;
    const limits = { ip: 8, email: emailCap };
    const dk = dayKey(now);
    const rl = await peekRateLimit(env.INTAKE_KV, { ip, email, dayKeyStr: dk, limits });
    if (!rl.allowed) return json({ ok: false, error: rl.reason }, 429);

    const approved = flags.length === 0;
    const issue = formatIssue({ ...clean, email: maskEmail(email) }, { author: env.AUTHOR_LOGIN, flags, approved });
    const created = await createIssue({ owner: env.GITHUB_OWNER, repo: env.GITHUB_REPO, token: env.GITHUB_TOKEN, ...issue }, fetchImpl);
    if (!created.ok) return json({ ok: false, error: "issue_create_failed" }, 502);

    await commitRateLimit(env.INTAKE_KV, { ip, email, dayKeyStr: dk, limits });
    await env.INTAKE_KV.put(`sub:${created.number}`, email, { expirationTtl: 60 * 60 * 24 * 60 });
    return json({ ok: true, approved });
  } catch {
    return json({ ok: false, error: "internal" }, 500);
  }
}
```

- [ ] **Step 4: 跑测试确认绿** — `bun test services/intake-worker/src/handler.test.js`

- [ ] **Step 5: 提交**

```bash
git add services/intake-worker/src/handler.js services/intake-worker/src/handler.test.js
git commit -m "feat(intake): 提交改造——token 鉴权、去 turnstile、红旗分流 approved/pending"
```

## Task A8: `index.js` 路由 + `wrangler.toml` 配置

**Files:**
- Modify: `services/intake-worker/src/index.js`
- Modify: `services/intake-worker/wrangler.toml`

- [ ] **Step 1: 改 `index.js`**

```js
import { handleIntake } from "./handler.js";
import { handleSubRead } from "./sub-read.js";
import { handleAdmin } from "./admin.js";
import { handleVerify } from "./verify.js";

export default {
  async fetch(request, env) {
    const { pathname } = new URL(request.url);
    if (pathname.startsWith("/sub/")) return handleSubRead(request, env);
    if (pathname.startsWith("/admin/")) return handleAdmin(request, env);
    if (pathname === "/verify") return handleVerify(request, env);
    return handleIntake(request, env);
  },
};
```

- [ ] **Step 2: 改 `wrangler.toml`** — `[vars]` 增：

```toml
MAX_PER_EMAIL_PER_DAY = "5"
ADMIN_MAX_FAILS_PER_HOUR = "10"
```
并把机密注释行补上 `ADMIN_KEY`：`#   TURNSTILE_SECRET, GITHUB_TOKEN, SUB_READ_SECRET, ADMIN_KEY`

- [ ] **Step 3: 跑全 Worker 测试确认绿** — `bun test services/intake-worker`

- [ ] **Step 4: 提交**

```bash
git add services/intake-worker/src/index.js services/intake-worker/wrangler.toml
git commit -m "feat(intake): 路由接入 /admin、/verify；wrangler 加额度/限流 vars"
```

---

# 阶段 B · 前端

## Task B1: `submit.js` 纯逻辑改造

**Files:**
- Modify: `web/src/assets/submit.js`
- Modify: `web/src/assets/submit.test.js`

- [ ] **Step 1: 改/加测试** `submit.test.js`：

```js
test("buildPayload：带 k，不含 email/turnstile", () => {
  const p = buildPayload({ title: " 稳定币 ", focus: "机制", message: "" }, "TOK");
  expect(p).toEqual({ k: "TOK", title: "稳定币", focus: "机制", message: "" });
});
test("tokenFromQuery：从 ?k= 取 token；无则空串", () => {
  expect(tokenFromQuery("?k=abc123")).toBe("abc123");
  expect(tokenFromQuery("")).toBe("");
  expect(tokenFromQuery("?x=1")).toBe("");
});
test("describeResult：unauthorized 文案", () => {
  expect(describeResult({ ok: false, error: "unauthorized" }).text).toContain("专属链接");
});
test("describeVerify：有效回显邮箱、无效给提示", () => {
  expect(describeVerify({ ok: true, email: "b***@x.com" }).authorized).toBe(true);
  expect(describeVerify({ ok: false }).authorized).toBe(false);
});
```

- [ ] **Step 2: 跑测试确认失败** — `bun test web/src/assets/submit.test.js`

- [ ] **Step 3: 改实现** `submit.js`：
  - `buildPayload(fields, token)` → `{ k: token||"", title, focus, message }`（删 email/turnstile）；
  - 加 `tokenFromQuery(search)`：`new URLSearchParams(search).get("k") || ""`；
  - 加 `describeVerify(res)`：`{ authorized: !!(res&&res.ok), email: res&&res.email||"" , text: ... }`；
  - `describeResult` map：删 `turnstile_failed`；加 `unauthorized: "需要专属链接才能提交，请联系作者获取你的专属链接。"`；`invalid` 文案改"请检查：题目必填、长度别超限。"

- [ ] **Step 4: 跑测试确认绿** — `bun test web/src/assets/submit.test.js`

- [ ] **Step 5: 提交**

```bash
git add web/src/assets/submit.js web/src/assets/submit.test.js
git commit -m "feat(web): submit 纯逻辑——带 token 提交、tokenFromQuery、verify 文案"
```

## Task B2: `index.template.html` 模态框改造

**Files:**
- Modify: `web/src/index.template.html`

- [ ] **Step 1: 改模板**（无单测，靠构建 sanity + 手测）：
  - 删 `<head>` 里 Turnstile 的 `<script>onTurnstileReady…</script>` 与 `<script src="https://challenges.cloudflare.com/turnstile/...">`。
  - 删表单里"你的邮箱"整个 `<label class="field">…email…</label>` 块。
  - 删 `<div id="ts-widget" …></div>`。
  - 在 `<form>` 顶部加授权态条：`<p id="auth-state" class="modal-hint" role="status" aria-live="polite" hidden></p>`。
  - `<form>` 上加 `data-verify="{{WORKER_URL}}/verify"`（已有 `data-worker`）。
  - "你留的邮箱"等文案改为"你的专属链接对应的邮箱"。

- [ ] **Step 2: 构建确认不崩** — `bun run build`（在 repo 根；Expected: `Built N entries`）

- [ ] **Step 3: 提交**

```bash
git add web/src/index.template.html
git commit -m "feat(web): 模态框去邮箱字段/去 Turnstile，加授权态条"
```

## Task B3: `feed.js` 接入 token 鉴权

**Files:**
- Modify: `web/src/assets/feed.js`

- [ ] **Step 1: 改 `feed.js`**：
  - import 增 `tokenFromQuery, describeVerify`。
  - 删 Turnstile 相关：`renderTurnstile`、`window.__renderTurnstile`、`tsId`、open() 里的 `renderTurnstile()`、submit 里取 token/reset 的逻辑。
  - 顶部读 token：`const TOKEN = tokenFromQuery(location.search);`
  - 新增 `verifyToken()`：若 `TOKEN` 为空 → 授权态 false；否则 `fetch(form.dataset.verify + "?k=" + encodeURIComponent(TOKEN))` → `describeVerify`。把结果写进 `#auth-state` 与一个模块级 `authorized` 标志；未授权时：`#auth-state` 显示"需要专属链接才能提交…"、禁用提交按钮、表单输入禁用。
  - `open()` 调 `verifyToken()`（或页面载入即验、缓存结果）。
  - submit handler：`buildPayload({title,focus,message}, TOKEN)`；移除 turnstile token 读取；其余（在途禁用、查重、文案）不变。

- [ ] **Step 2: 构建确认不崩 + 资产被复制** — `bun run build`；确认 `web/dist/assets/feed.js` 存在且不含 `turnstile`（`grep -c turnstile web/dist/assets/feed.js` 期望 0）。

- [ ] **Step 3: 提交**

```bash
git add web/src/assets/feed.js
git commit -m "feat(web): 提交弹窗按 ?k= 鉴权——verify 后才可提交，去 Turnstile"
```

## Task B4: 管理页 `admin.html` + `admin.js`

**Files:**
- Create: `web/src/admin.template.html`
- Create: `web/src/assets/admin.js`
- Create: `web/src/assets/admin.test.js`
- Modify: `web/build/build.js`

- [ ] **Step 1: 写 `admin.js` 纯逻辑测试** `admin.test.js`：

```js
import { test, expect } from "bun:test";
import { inviteLink, renderPeopleRows, describeAdminError } from "./admin.js";

test("inviteLink：按站点 origin+base 拼链接", () => {
  expect(inviteLink("https://qiuyuanqr.github.io/searchX/", "TOK")).toBe("https://qiuyuanqr.github.io/searchX/?k=TOK");
});
test("renderPeopleRows：转义邮箱、含撤销按钮 + 链接", () => {
  const html = renderPeopleRows([{ email: "a@x.com", token: "T", addedAt: 0 }], "https://s/");
  expect(html).toContain("a@x.com");
  expect(html).toContain("?k=T");
  expect(html).toContain("data-email=\"a@x.com\"");
});
test("renderPeopleRows：邮箱里的 HTML 被转义防 XSS", () => {
  const html = renderPeopleRows([{ email: "<img>@x.com", token: "T", addedAt: 0 }], "https://s/");
  expect(html).not.toContain("<img>");
});
test("describeAdminError：401/429 给清楚中文", () => {
  expect(describeAdminError(401)).toContain("密钥");
  expect(describeAdminError(429)).toContain("锁定");
});
```

- [ ] **Step 2: 跑测试确认失败** — `bun test web/src/assets/admin.test.js`

- [ ] **Step 3: 实现 `admin.js`**（纯逻辑：拼链接 / 渲染 / 文案；DOM 引导内联在模板的 `<script type=module>` 里 import 这些）

```js
export function escapeHtml(s){ return String(s==null?"":s).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;"); }
export function inviteLink(base, token){ return new URL("?k=" + encodeURIComponent(token), base).href; }
export function renderPeopleRows(people, base){
  return people.map((p) => {
    const email = escapeHtml(p.email);
    const link = escapeHtml(inviteLink(base, p.token));
    return `<tr><td>${email}</td><td><input class="search" readonly value="${link}"></td>`
      + `<td><button class="linklike" data-act="copy" data-link="${link}">复制</button> `
      + `<button class="linklike" data-act="revoke" data-email="${email}">撤销</button></td></tr>`;
  }).join("");
}
export function describeAdminError(status){
  if (status === 401) return "密钥不对，请重输管理密钥。";
  if (status === 429) return "尝试过多已临时锁定，请稍后再试。";
  return "操作失败，请重试。";
}
```

- [ ] **Step 4: 实现 `admin.template.html`**（纯密钥闸：未输密钥前只显示密钥输入；零第三方脚本；密钥存 localStorage、走 `x-admin-key` 头）。关键骨架：

```html
<!doctype html><html lang="zh-CN"><head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
<title>searchX · 授权管理</title><link rel="stylesheet" href="assets/feed.css">
<meta name="robots" content="noindex">
</head><body data-pagefind-ignore>
<div class="wrap">
  <h1 class="brand">授权管理</h1>
  <div id="gate"><p class="lead">输入管理密钥</p>
    <input id="admin-key" class="search" type="password" autocomplete="off" placeholder="ADMIN_KEY">
    <button id="enter" class="submit-btn" type="button">进入</button>
    <p id="gate-msg" class="form-status" hidden></p>
  </div>
  <div id="panel" hidden>
    <form id="add-form"><input id="add-email" class="search" type="email" placeholder="加一个授权邮箱"><button class="submit-btn" type="submit">生成专属链接</button></form>
    <p id="add-msg" class="form-status" hidden></p>
    <table class="people"><tbody id="people"></tbody></table>
  </div>
</div>
<script type="module">
import { inviteLink, renderPeopleRows, describeAdminError } from "./assets/admin.js";
const WORKER = "{{WORKER_URL}}";
const base = new URL(".", document.baseURI).href;        // 站点根（admin.html 同级）
let key = localStorage.getItem("searchx_admin_key") || "";
const $ = (id) => document.getElementById(id);
async function api(path, opts={}){ return fetch(WORKER+path, { ...opts, headers: { "content-type":"application/json", "x-admin-key": key, ...(opts.headers||{}) } }); }
async function load(){ const r = await api("/admin/list"); if(!r.ok){ throw r.status; } const j = await r.json(); $("people").innerHTML = renderPeopleRows(j.people||[], base); }
async function enter(){ key = $("admin-key").value.trim(); try { await load(); localStorage.setItem("searchx_admin_key", key); $("gate").hidden=true; $("panel").hidden=false; } catch(s){ $("gate-msg").textContent=describeAdminError(s); $("gate-msg").hidden=false; } }
$("enter").addEventListener("click", enter);
$("add-form").addEventListener("submit", async (e)=>{ e.preventDefault(); const email=$("add-email").value.trim(); const r=await api("/admin/add",{method:"POST",body:JSON.stringify({email})}); const j=await r.json().catch(()=>({})); if(j.ok){ $("add-email").value=""; await load(); } else { $("add-msg").textContent = r.status===400?"邮箱格式不对":describeAdminError(r.status); $("add-msg").hidden=false; } });
$("people").addEventListener("click", async (e)=>{ const b=e.target.closest("button"); if(!b) return; if(b.dataset.act==="copy"){ navigator.clipboard.writeText(b.dataset.link); b.textContent="已复制"; } if(b.dataset.act==="revoke"){ if(!confirm("撤销 "+b.dataset.email+"？该链接立即失效")) return; await api("/admin/remove",{method:"POST",body:JSON.stringify({email:b.dataset.email})}); await load(); } });
if (key) enter();                                          // 已存密钥则自动进
</script>
</body></html>
```

- [ ] **Step 5: 改 `build.js`** — 在产出 `submit.html` 附近加 admin 页产出：

```js
// 管理页（授权白名单）：注入 WORKER_URL，noindex；不在站内任何处放入口链接
const adminTpl = readFileSync("web/src/admin.template.html", "utf8");
writeFileSync(join(out, "admin.html"), injectConfig(adminTpl, cfg));
```
（同时把 `adminTemplate` 作为 `build()` 形参，默认 `"web/src/admin.template.html"`，与 `submitTemplate` 风格一致。）

- [ ] **Step 6: 跑测试 + 构建确认** — `bun test web/src/assets/admin.test.js` 绿；`bun run build` 后确认 `web/dist/admin.html` 存在且含 `searchx-intake`（WORKER_URL 已注入）。

- [ ] **Step 7: 提交**

```bash
git add web/src/admin.template.html web/src/assets/admin.js web/src/assets/admin.test.js web/build/build.js
git commit -m "feat(web): 授权管理页（纯密钥闸 + 加人/列表/撤销/复制链接），noindex"
```

## Task B5: 构建 sanity + 文案/文档收尾

**Files:**
- Modify: `web/build/build.test.js`（若有 admin 产出断言）
- Modify: `web/src/assets/submit.js`（确认无遗漏 turnstile 文案）
- Modify: `services/intake-worker/README.md` + `web/README.md`（更新流程说明）

- [ ] **Step 1: 加构建断言** — `build.test.js` 增："产出 admin.html 且含 WORKER_URL"（参照现有 submit.html 断言风格）。
- [ ] **Step 2: 更新两个 README** — intake-worker README 增"授权白名单 / token 链接 / 管理页 / ADMIN_KEY"段；web README 提到 admin 页与 noindex；删过时的"作者贴 approved"为唯一闸的旧表述（改为"授权用户自动放行、可疑件降级人工"）。
- [ ] **Step 3: 跑全量测试** — `bun test`（Expected: 全绿）。
- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "docs+test: 构建 sanity 覆盖 admin 页；README 更新授权放行流程"
```

---

# 阶段 C · 验证与部署（人工）

- [ ] **C1 全量测试绿**：`bun test`。
- [ ] **C2 对抗式复审（Workflow）**：跑安全 + 正确性多代理复审（鉴权绕过 / 凭证隔离 / KV 键注入 / 红旗分流 / CORS / XSS），修掉确认项。
- [ ] **C3 部署 Worker（作者手动）**：`bun run build:worker` → Cloudflare 控制台粘 `dist/worker.js`；在 Variables/Secrets 加 `ADMIN_KEY`（`openssl rand -hex 24`），确认 vars `MAX_PER_EMAIL_PER_DAY`/`ADMIN_MAX_FAILS_PER_HOUR`。
- [ ] **C4 前端上线**：push → CI 自动部署（含 `admin.html`）。
- [ ] **C5 端到端自测**：打开 `/admin.html` 输 ADMIN_KEY → 加一个自己的测试邮箱 → 复制专属链接 → 无痕打开 `/?k=<token>` → 提交一条 → 确认建了 `approved` issue → runner 自动跑上线。再测：无 k 打开 → 提交被锁；错 ADMIN_KEY → 401/锁定。

---

## 自查（spec 覆盖 / 占位 / 类型一致）

- **spec 覆盖**：① token 链接=B1/B3 + A2/A4；② 锁死公开提交=A7(403)+B3(禁用)；③ 红旗降级=A6/A7；④ 每日上限=A7；⑤ 永不过期=A2（不设 TTL）；⑥ 管理员密钥闸+限流+隔离=A3+B4。逐条有任务。
- **占位**：无 TODO/TBD；每步给了实际代码/命令/期望。
- **类型一致**：`mintInvite`/`emailForToken`/`listPeople`/`revoke`/`rotate` 签名贯穿 A2→A3→A4→A7 一致；`buildPayload(fields, token)`→`{k,...}` 贯穿 B1→B3；`formatIssue(…, {author,flags,approved})` 贯穿 A6→A7；`{{WORKER_URL}}` 注入贯穿 A8/B2/B4。
